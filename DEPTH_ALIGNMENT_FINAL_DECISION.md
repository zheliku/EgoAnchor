# 深度对齐度评估 - 最终决策总结

## 决策：回归V1极简方案

经过深入讨论，我们决定**不采用复杂的V2方案**，而是保持V1的简洁设计。

---

## 📊 核心判断依据

### 论文的真正核心贡献

根据论文全文分析，EgoAnchor的核心贡献是：

1. **分层架构** - 视觉感知后端 + 对象锚定运行时解耦
2. **时空对齐** - 帧溯源消除动态配准误差（RQ1）
3. **可靠性感知静止锁** - 稳定性与响应性平衡（RQ2）
4. **系统集成与用户研究** - 真实场景可用性（RQ3）

**深度对齐度D**：
- 只是VCD可靠性评分（V × G_CD）的一个子项
- 作用是"过滤坏观测"，不是核心贡献
- 论文结论明确说："系统的核心并非提升单帧位姿精度"

---

## ❌ 为什么不用V2复杂方案

### V2的问题

1. **抢主线** - 让论文看起来像"深度几何评估metric设计"，而非"MR锚定系统"
2. **增加攻击面**：
   - 当前实现的"Chamfer"不是真正的双向点到点Chamfer
   - 梯度一致性≠表面法向，理论最弱
   - 需要justify权重选择（0.35/0.40/0.25）
3. **不符合定位** - 深度对齐度应该是"工程性confidence estimate"，不是"新算法贡献"

### GPT的关键建议

> "从论文策略上看，你们不应该在深度几何对齐度上做复杂方案。这部分只需要满足三个标准：
> 1. 能过滤明显错误位姿
> 2. 能解释为什么适合消费级双目PMR
> 3. 不抢走核心贡献的注意力"

---

## ✅ 最终采用的方案

### 极简V1公式

```
D = λ·ρ_inlier + (1-λ)·S_med

其中：
ρ_inlier = (1/|Ω|) Σ 𝟙[|ΔD(p)| < τ(p)]
S_med = 1 - median(min(|ΔD(p)|/τ(p), 1))
τ(p) = max(τ_min, ρ_z · D_rnd(p))
```

权重：λ = 0.6

### 优势

1. **简单可信** - inlier ratio + median是标准做法，审稿人不会质疑
2. **物理直观** - 距离自适应阈值有明确物理意义
3. **易于解释**：
   - ρ_inlier：有多少区域几何对齐
   - S_med：对齐区域的典型误差有多小
4. **不分散注意力** - 让论文聚焦核心贡献

---

## 📝 论文表述策略

### 篇幅控制

- 可靠性评分章节：≤ 1页
- 深度对齐度：20-30行
- 重点：V × G(C,D) 的整体设计
- 定位：工程性实现，不是理论贡献

### 文字描述（论文版）

> "深度几何对齐度 $D$ 度量观测深度与渲染深度的空间一致性。双目立体重建的深度不确定性随距离呈二次方增长，因此采用距离自适应的内点容差。在有效交集区域，通过内点比例 $\rho_{\text{inlier}}$ 与归一化残差中位数 $S_{\text{med}}$ 联合评估。内点比例刻画几何覆盖率，归一化中位数提供对离群点鲁棒的典型误差估计。"

### 评估策略

在RQ2中轻量消融：
- without reliability gating
- with reliability gating

报告对**异常尖刺比例**、**静止期抖动**、**屏幕空间漂移**的影响。

**不做**：
- 与Chamfer/SSIM/normal consistency的对比
- 权重敏感性分析
- 独立的metric validation

---

## 🗑️ 已删除的内容

### 删除的文件
- ✅ `depth_alignment_v2.py`
- ✅ `test_depth_alignment_v2.py`
- ✅ `verify_depth_alignment_v2.py`
- ✅ `depth_alignment_v2_guide.md`
- ✅ V2相关配置项

### 删除的论文表述
- ❌ "Chamfer距离项"
- ❌ "分布特征项（IQR）"
- ❌ "梯度一致性项"
- ❌ 三维加权公式
- ❌ 长篇幅的子项解释

---

## 💡 经验教训

### 什么是过度设计

最初我提出的V2方案包含：
- Chamfer距离
- 分布特征（中位数+IQR）
- 深度梯度一致性
- 多维加权融合

这些在理论上都合理，但**与论文定位不匹配**。

### 正确的思考方式

论文写作不是"堆砌所有可能的技术改进"，而是：
1. **明确核心故事** - EgoAnchor是锚定系统，不是感知评估
2. **分清主次** - 时空对齐和静止锁是主角，深度评分是配角
3. **控制复杂度** - 每个组件都要justify，复杂度是成本

### 审稿人视角

审稿人会问：
- "为什么这个系统work？" ✅ 时空对齐 + 静止锁
- "实验证明了什么？" ✅ RQ1/RQ2/RQ3

审稿人**不会**问：
- "为什么深度对齐不用Chamfer？"（除非你强调它）
- "为什么权重是0.6？"（除非你claim这是贡献）

---

## 🎯 下一步工作

现在深度对齐度已确定，应该专注于：

1. **完善评估章节**（RQ1/RQ2/RQ3）
2. **准备用户研究**
3. **完善讨论与局限**
4. **准备补充材料**

**不要再**：
- 纠结深度评分的细节
- 尝试更复杂的metric
- 做不必要的消融实验

---

## 📌 最终公式（论文版）

### 可靠性评分

```
R = V · G_CD

其中：
V = |M_obs| / |M_rnd|  (可视度因子)

G_CD = exp((w_C·ln(max(C,ε)) + w_D·ln(max(D,ε))) / (w_C + w_D))

w_C = 0.2, w_D = 0.8, ε = 0.05
```

### 深度几何对齐度

```
D = λ·ρ_inlier + (1-λ)·S_med

ρ_inlier = (1/|Ω|) Σ_{p∈Ω} 𝟙[|D_rnd(p) - D_obs(p)| < τ(p)]

S_med = 1 - median_{p∈Ω}(min(|D_rnd(p) - D_obs(p)| / τ(p), 1))

τ(p) = max(τ_min, ρ_z · D_rnd(p))

λ = 0.6, τ_min = 5mm, ρ_z = 0.02
```

---

## 总结

**简单，就是最好的策略。**

深度对齐度V1已经足够支撑整个系统运行，不需要更复杂的设计。

让论文聚焦真正的核心贡献：**时空对齐**和**可靠性感知静止锁**。

---

**决策时间**: 2026-07-01  
**状态**: ✅ 已完成并确认  
**后续行动**: 专注论文核心章节
