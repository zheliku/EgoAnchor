1. python侧应该以包为单位进行导入，而不是具体到模块中的某个文件。例如：from egoanchor.algorithms import ...，而不是 from egoanchor.algorithms.xxx.py import ...。

2. 代码需要有详细的中文注释。如果是配置文件 .toml，每个参数的末尾都要写明注释（不换行注释）。如果是类，其变量成员和每个方法也需要添加中文注释。

3. 代码命名规范，不要过于冗长，能清楚的表达意思即可。