1. python侧应该以包为单位进行导入，而不是具体到模块中的某个文件。例如：包内使用**from .image_utils import fit_to_size, stack_stereo** 这种显式 re-export，包外则使用 from egoanchor.algorithms import ...，而不是 from egoanchor.algorithms.xxx.py import ...。不要使用包级懒导出。
2. 代码需要有详细的中文注释。如果是配置文件 .toml，每个参数的末尾都要写明注释（不换行注释）。如果是类，其变量成员和每个方法也需要添加中文注释。
3. 代码命名规范，不要过于冗长，能清楚的表达意思即可。
4. 修改代码时不要考虑仅是打补丁，而是全局项目考虑，考虑代码之间的配合、引用、项目架构等，不要东补一些西补一些。
5. 请你仔细阅读我的项目，按照计划严格执行并实现代码
6. 重构代码时不要兼容旧代码！不要兼容旧代码！不要兼容旧代码！
