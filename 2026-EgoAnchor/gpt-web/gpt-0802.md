
## 结论

Fable5 的分析**方向基本正确，但需要一次关键校准**。

它正确识别了你想要的系统论文叙事：先展示 EgoAnchor 带来的系统能力，再解释这些能力由哪些设计实现，最后用定量实验、消融和用户研究建立证据。这个方向确实比当前稿件中以 G1/G2/G3 为中心的“问题形式化—逐项解决”路线更接近 VRGaussianAvatar。VRGaussianAvatar 的有效之处也并不是单纯“写得像系统论文”，而是它始终维持了清晰的贡献层级：**一个完整系统、一项有辨识度的核心技术、以及定量与感知评价**。

但 Fable5 有一个根本问题：

> 它把“开放易部署、面向日常物体、稳定动态锚定”当成了三个同等强度、且分别由三个模块和实验验证的贡献。

这会产生新的叙事风险。最终论文不应是三个卖点并列，而应建立**一个主贡献、两个系统属性和若干支撑技术**。

---

# 一、Fable5 分析中合理的部分

## 1. 从“形式化问题”转向“展示系统能力”

这一判断正确。当前稿件的 G1/G2/G3 具有内部设计价值，但不适合成为全文最醒目的叙事框架。现在的引言读起来像：

> 我们识别了三个缺口，然后分别设计三个机制解决它们。

高质量系统论文更自然的表达是：

> 我们构建了一个以往难以实现的系统能力；为了实现该能力，我们做出了若干关键设计。

因此，G1/G2/G3 可以保留为作者内部的逻辑检查，但不应继续作为正文中的显式标签，也不应在结果中反复出现“解决 G1”“解决 G3-稳定性”。

## 2. 方法章节需要从“用了什么”转向“为什么这样设计”

Fable5 对这一点的判断也正确。

例如，当前稿件对 Kalman、Linear/SLERP、StaticLock 和生命周期分别进行技术说明，但各部分之间的系统动机不够集中。更好的写法不是：

> 系统采用卡尔曼滤波。系统采用历史插值。系统采用 StaticLock。

而是：

> 固定参数平滑无法同时满足静止稳定和运动响应。EgoAnchor 因此根据物体的运动状态改变锚点生成策略：运动期间持续合成轨迹，静止期间锁定稳定参考，遮挡期间保持最后可靠锚点，并在观测恢复后重新接纳更新。

这会把多个工程组件提升为一项统一的系统设计。

## 3. 实验需要由图和结论驱动

这一点同样正确。当前实验的指标较完整，但阅读体验接近测试报告。每一小节都应先给出一句明确结论，再给图和关键数字，最后解释原因。

例如：

> EgoAnchor 显著改善了静止锚定行为：相较不使用 StaticLock 的配置，其中心化静止误差降低了 17.06 倍，且所有 12 个配对片段均获得改善。

而不是先依次罗列四种配置的多个统计量，再让读者自行寻找结论。

---

# 二、Fable5 分析中需要修正的部分

## 1. 三个优势不能作为三个等权主贡献

你提出的三个特点确实都重要：

1. 无标记、无需逐物体训练；
2. 支持不同日常刚性物体；
3. 提供稳定、世界一致、可恢复的动态锚点。

但它们在论文中的学术地位并不相同。

其中：

* **稳定动态锚定**是核心技术贡献；
* **无需标记和逐物体训练**是系统接入方式；
* **支持不同刚性物体**是系统适用范围。

最终层级应当是：

> **主贡献：**EgoAnchor 是一个基于零样本视觉感知的稳定动态物体锚定系统。
> **系统属性：**它不依赖物理标记或逐物体训练，并可以在给定三维模型时接入不同类别的刚性物体。
> **支撑技术：**零样本感知流水线、VCD 位姿验证和运动状态感知锚定运行时。

否则，引言会同时承诺“部署”“泛化”“精度”“稳定”“恢复”“用户信任”，审稿人会要求每一项都得到独立且充分的验证。

## 2. “Open & Deployable”目前表述过强

当前系统仍然需要：

* 目标物体的三维模型；
* 外部 RTX 3090 工作站；
* Wi-Fi 网络；
* 针对每个物体启动对应的感知后端。

因此，不建议把 **Open & Deployable** 作为正式术语。它容易让审稿人理解为：

* 头显端独立运行；
* 无需对象先验；
* 即装即用；
* 已经具备广泛实际部署条件。

更准确的表述是：

### 中文

**低准备成本的无标记接入**

> 给定目标物体的三维模型，系统无需物理标记、专用追踪器或逐物体模型训练。

### 英文

**Markerless and per-object-training-free onboarding**

或者更简洁地使用：

**Zero-shot object onboarding**

“Deployable”可以在讨论中作为发展方向，而不应成为当前版本的首要 claim。

## 3. “支持任意日常刚性物体”证据不足

目前用户实验涉及鼠标、订书机和游戏手柄。三种物体在形态和交互方式上有差异，但不足以支持：

> arbitrary everyday rigid objects
> general-purpose object anchoring

更安全且仍然有价值的表述是：

> **mesh-conditioned, category-agnostic support for diverse rigid objects**

中文可写为：

> **给定可用三维模型后，系统不受预定义类别集合限制，可接入不同类别的日常刚性物体。**

注意其中三个限定：

* 给定三维模型；
* 不受预定义类别集合限制；
* 已在若干不同物体上验证。

除非补充更系统的跨物体实验，否则不要使用“任意物体”“通用对象锚定”或“广泛泛化”。

## 4. 0.82 mm 不能写成“毫米级精度”

这是当前叙事中最需要严格控制的地方。

0.82 mm 对应的是**中心化静止平移泄漏或静止误差**，反映系统在静止阶段的稳定性，并不是绝对6DoF注册精度。当前稿件同时报告的绝对注册中位数是 6.60 mm。

因此可以写：

> EgoAnchor achieved sub-millimeter platform-relative static leakage.

或者：

> EgoAnchor 将中心化静止平移误差降低至 0.82 mm。

但不应直接写：

> EgoAnchor achieves 0.82-mm tracking accuracy.

建议在全文中建立固定术语：

* **static leakage / static stability**：0.82 mm；
* **absolute registration error**：6.60 mm；
* **lag-aligned dynamic residual**：9.12 mm；
* **current-time dynamic error**：125.83 mm。

这样既能突出结果，也不会给审稿人留下指标包装的印象。

## 5. StaticLock 没有“打破”稳定性—响应性权衡

Fable5 建议写“StaticLock 打破抖动—延迟权衡”，这一措辞过强。

你们的数据中，StaticLock 的确显著改善静止稳定性，但也带来了约 510 ms 的起动转换代价。因此，它不是消除了权衡，而是：

> **通过运动状态感知策略重新管理稳定性与响应性的权衡。**

推荐写法：

> 与对所有运动状态使用统一滤波策略不同，EgoAnchor 在检测到物体静止时锁定稳定参考，而在持续运动出现后恢复轨迹更新。这一状态相关策略显著降低静止抖动，同时以可测量的起动转换延迟为代价。

这一表述更准确，也更有系统设计价值。

## 6. 用户实验不能验证“开放易部署”和“泛化能力”

实验三可以支持：

* 不同日常物体上的系统可运行性；
* 用户能够感知锚点稳定性和恢复差异；
* 这些差异影响可靠性感知和依赖意愿。

但它不能直接证明：

* 系统易部署；
* 系统支持任意物体；
* 零样本感知成功率高；
* 新物体接入成本低。

因此，Fable5 提出的“实验三验证优势1+2”并不严谨。

要真正支持接入范围和部署成本，最好增加一个紧凑的**对象覆盖实验**：

* 选择约 10–15 个不同日常刚性物体；
* 覆盖不同尺寸、纹理、几何复杂度和材质；
* 报告初始化成功率；
* time-to-first-anchor；
* 有效候选率；
* 追踪可用率；
* 遮挡后恢复成功率；
* 每个物体需要的准备内容和时间。

这个实验不一定需要外部真值，一张物体 gallery 加一张紧凑表格即可。如果不能增加，应将论文表述收缩为“demonstrated across three everyday rigid objects”。

## 7. VCD 是否能作为独立贡献，取决于评价强度

目前 VCD 的 risk–coverage 结果说明其分数具有一定排序价值，但如果要把 VCD 写成独立方法贡献，建议至少比较：

* visibility only；
* color only；
* depth only；
* color + depth 的线性融合；
* VCD 几何融合；
* 位姿估计模块自身的置信指标或简单深度残差。

否则，审稿人可能认为它只是一个合理的工程门控函数。

在当前证据下，较稳妥的定位是：

> **VCD 是 EgoAnchor 中针对跨物体质量波动设计的位姿验证机制。**

它可以是核心系统技术，但不必单独宣称为通用置信度估计算法。

---

# 三、最终推荐的论文定位

## 一句话定义

> **EgoAnchor 是一套面向消费级混合现实的零样本动态物体锚定系统：给定目标物体的三维模型，它无需物理标记或逐物体训练，即可将头显双目图像中的异步6DoF视觉观测转化为稳定、世界一致且可恢复的对象锚点。**

对应英文：

> **EgoAnchor is a zero-shot dynamic object anchoring system for consumer mixed reality that converts asynchronous visual pose observations into stable, world-consistent, and recoverable object anchors, without physical markers or per-object training.**

这句话同时覆盖了：

* 系统是什么；
* 输入条件是什么；
* 不需要什么；
* 核心技术效果是什么。

## 推荐标题

首选：

> **EgoAnchor：面向消费级混合现实的零样本稳定动态物体锚定系统**

英文：

> **EgoAnchor: A Zero-Shot System for Stable Dynamic Object Anchoring in Consumer Mixed Reality**

另一个更强调接入方式的版本：

> **EgoAnchor: Markerless Dynamic Object Anchoring for Everyday Mixed Reality with Zero-Shot Perception**

第一版更稳妥，也更接近 VRGaussianAvatar 的系统标题风格。

---

# 四、最终叙事不应是“三条优势机械映射”，而应是一个主线

不建议采用：

```text
痛点1 → 优势1 → 模块1 → 实验1
痛点2 → 优势2 → 模块2 → 实验2
痛点3 → 优势3 → 模块3 → 实验3
```

这种结构虽然整齐，但容易再次写成需求追踪表。

最终应采用以下主线：

```text
零样本视觉位姿让更多真实物体可以被定位
                    ↓
但视觉位姿本身不足以成为可用的动态MR锚点
                    ↓
EgoAnchor将零样本感知与专门的锚定运行时集成为完整系统
                    ↓
系统无需标记和逐物体训练，并支持不同刚性物体
                    ↓
VCD与状态感知运行时使锚点在静止、运动、遮挡和恢复中稳定工作
                    ↓
定量实验解释性能，消融解释原因，用户研究证明感知价值
```

其中：

* **零样本、无标记和跨类别支持**定义系统的覆盖范围；
* **稳定动态锚定**定义系统的核心技术价值；
* **用户信任**定义系统的应用价值。

---

# 五、推荐的全文组织

## 1. Introduction

建议六段，不使用 G1/G2/G3 标签。

### 第一段：应用需求

动态真实物体上的虚拟内容附着是对象中心 MR 的基础能力。

### 第二段：现有方案不足

* 标记或专用追踪器需要改造物体；
* 平台能力对象范围和运行时行为受限；
* 零样本视觉位姿扩大了对象覆盖范围，但没有直接产生应用可用锚点。

### 第三段：系统挑战

用一句核心判断即可：

> 视觉位姿估计回答物体在某个历史图像时刻相对于相机的位置，而混合现实应用需要在每个渲染时刻获得世界坐标系下持续、稳定且具有生命周期语义的对象锚点。

随后简述异步、质量波动、低频和遮挡问题，不编号。

### 第四段：EgoAnchor

介绍：

* Quest 3 双目输入；
* 零样本感知后端；
* VCD 验证；
* 锚定运行时；
* 静止、运动、遮挡和恢复行为。

### 第五段：结果预览

建议选三个最有力且语义不同的结果：

* capture-time alignment：2.69 倍；
* StaticLock：17.06 倍；
* 用户可靠性或信任选择：18/24。

0.82 mm 可以写成“sub-millimeter static leakage”，不能写成整体精度。

### 第六段：三项贡献

---

## 2. Related Work

保留三节：

1. 真实物体追踪与锚定；
2. 零样本6DoF位姿估计；
3. XR中的异步感知、滤波与时延处理。

平台产品介绍应压缩，重点用于建立设计空间，而不是逐个平台描述功能。

---

## 3. EgoAnchor System

### 3.1 System Overview

系统输入、输出、前后端结构和对象接入流程。

建议明确：

> 输入：双目图像、历史相机位姿、目标语义和三维模型。
> 输出：世界系对象锚点、追踪状态和可用性状态。

### 3.2 Zero-Shot Object Perception

说明开放词表初始化、时序分割、双目深度和 FoundationPose 如何形成完整流水线。

重点不是分别介绍四个网络，而是解释：

> 为什么这些模块能够在无需逐物体训练的条件下，为不同形态的刚性物体生成米制6DoF观测。

### 3.3 VCD Pose Validation

保留方法思想和一个核心公式。通道实现、全部权重及细节进入补充材料。

### 3.4 Dynamic Anchor Runtime

建议内部重组为三部分：

1. **Capture-time world registration**
   恢复观测的正确世界坐标语义。
2. **Motion-state-aware anchor synthesis**
   运动时合成轨迹，静止时锁定锚点。
3. **Occlusion and lifecycle handling**
   在观测丢失时保持、退化和恢复。

Kalman、Linear/SLERP 和四路解锁均作为这些设计的实现机制，而不是独立贡献。

---

## 4. Implementation

控制在半页以内。

主文保留：

* Quest 3；
* RTX 3090 后端；
* 候选更新率；
* 渲染帧率；
* 局域网异步通信。

Python、CUDA、TensorRT、ZeroMQ、NATS 和全部阈值放入补充材料。当前版本中的若干软件版本也应重新核查，避免占用正文且产生可疑信息。

---

## 5. Evaluation

推荐采用四个研究问题，但可以仍然组织为三项实验。

### RQ1：EgoAnchor 能否接入不同日常刚性物体？

最好增加对象覆盖与接入开销评价。没有新增实验时，只做定性展示，并收缩泛化表述。

### RQ2：完整系统是否提供稳定动态锚点？

受控实验报告：

* 世界一致性；
* 静止稳定性；
* 动态轨迹保真度与时延；
* 遮挡保持与恢复。

### RQ3：各关键设计分别贡献了什么？

消融：

* capture vs. arrival；
* VCD；
* StaticLock；
* interpolation vs. extrapolation。

### RQ4：这些差异是否能够被用户感知？

用户研究报告：

* 静止稳定；
* 位置正确；
* 恢复一致；
* 可靠性；
* 依赖意愿。

用户实验不能用于证明任意物体泛化，只能证明跨三个物体的感知效用。

---

# 六、最终贡献列表建议

建议直接采用以下层级：

1. **零样本动态物体锚定系统。**
   我们提出 EgoAnchor，一套面向消费级混合现实的端到端系统。给定目标物体三维模型，系统通过头显双目图像实现无标记、无需逐物体训练的对象接入，并输出可供应用持续消费的世界系动态锚点。
2. **质量感知的锚点生成方法。**
   我们设计 VCD 位姿验证与运动状态感知锚定运行时，通过采集时刻世界配准、观测接纳、时序合成、静止锁定和生命周期管理，在静止、运动、遮挡及恢复期间维持稳定且可预测的锚点行为。
3. **分层系统评价。**
   我们通过具有平台参考的受控基准、关键组件消融和24人被试内研究评价系统，结果显示采集时刻对齐和静止锁定显著改善世界一致性与静止稳定性，并提升用户对位置正确性、恢复一致性、可靠性和依赖意愿的评价。

---

# 七、推荐摘要版本

> 动态真实物体锚定使虚拟内容能够随日常物体的拿取、移动和遮挡持续保持空间附着，是对象中心混合现实应用的基础能力。现有方案通常依赖物理标记、专用追踪设备或受限的平台对象集合。零样本6DoF位姿估计扩大了可被视觉定位的物体范围，但其输出是异步、低频且质量波动的相机系位姿观测，无法直接作为混合现实应用的对象锚点。本文提出EgoAnchor，一套面向消费级混合现实的零样本动态物体锚定系统。给定目标物体的三维模型，EgoAnchor无需物理标记或逐物体训练，通过开放词表初始化、时序分割、双目深度估计和模型驱动的6DoF位姿估计构建对象感知后端。系统进一步采用基于可视度、颜色和深度一致性的VCD机制验证位姿观测，并通过采集时刻世界配准、运动状态感知的时序合成、静止锚定和生命周期管理，将异步观测持续转化为稳定、世界一致且可恢复的对象锚点。我们在Meta Quest 3与外部GPU工作站上实现EgoAnchor，并通过受控系统基准、组件消融和24人被试内研究进行评价。结果表明，采集时刻对齐将头动相关误差降低2.69倍，静止锚定将中心化静止误差降低17.06倍；用户研究进一步显示，EgoAnchor在静止稳定性、位置正确性、恢复一致性、可靠性和依赖意愿方面获得更高评价。

---

# 八、必须先修正的数据问题

当前 v1 仍保留了已经被实验表格否定的旧数据：

* 参与者应为女性12名、男性12名；
* 年龄应为 (27.08\pm4.45) 岁；
* 总体偏好应为 EgoAnchor 15人、One-Euro 4人、无偏好5人；
* 信任选择应为 EgoAnchor 18人、One-Euro 1人、无偏好5人。

因此：

* **18/24 是信任选择，不是总体偏好；**
* 总体偏好是 15/24，即 62.5%；
* 当前 v1 中的 9/15、(24.8\pm3.1)、18/24偏好和19/24信任均需删除。

---

## 最终判断

Fable5 的总体方向值得采用，但不应照搬其“三个优势—三个模块—三个实验”的机械映射。

最终论文应形成以下层级：

> **系统主张：**EgoAnchor 实现了基于零样本视觉感知的稳定动态物体锚定。
> **接入属性：**无需物理标记或逐物体训练，给定三维模型即可支持不同类别的刚性物体。
> **关键技术：**零样本感知流水线、VCD 位姿验证和运动状态感知锚定运行时。
> **证据结构：**对象覆盖、受控性能、组件消融和用户感知。

这比单纯强调“系统有很多优点”更有说服力，也比以 G1/G2/G3 为主线更符合 VRGaussianAvatar 式的系统论文叙事。
