我审阅的是这份最新 TeX 手稿。

## 总体判断

你的注释判断基本正确，而且它们指向同一个问题：

> 当前第3章对“运行时如何执行”解释得过多，对“EgoAnchor为何构成一种动态物体锚定方法”强调得不够集中。

下一轮不应继续添加“轴、通路、跨轴状态、不变量”等概念。需要做结构性删减：

* 全文叙事上只有一条主线：**异步零样本位姿观测如何成为应用可消费的动态对象锚点**。
* 实现上确实有两类异步事件：**候选到达**和**渲染刷新**。
* 两类事件不是两个串行阶段，也不是两条数学时间轴；它们只是共享运行时状态的两个事件处理过程。
* 这一实现事实在 §3.3 和 Algorithm 中交代一次即可，不应进入摘要、贡献和结论的核心表述。

---

# 1. 系统概览：删除“三项目标”标签，但保留三个系统印象

你对“易部署、广适用、稳锚定”的判断是对的。当前写法读起来像项目需求，而且“易部署”与外部 GPU 工作站、三维模型要求并不完全匹配；“广适用”也容易被理解为广泛实证覆盖，但正式用户研究只有三个物体。

不建议保留三个斜体标签。应把三个特征转化为具体系统属性，自然嵌入概览。

建议用下面两段整体替换当前 §3.1 开头：

> EgoAnchor面向具有可用三维模型的刚性日常物体。新目标由文本提示与三维模型指定，无需物理标记或逐物体训练，对象范围也不受平台预定义类别集合限制。系统将动态物体锚定解耦为感知后端与锚定运行时：感知后端从头显双目图像中生成带采集时刻和可靠性评分的相机系6DoF位姿观测；锚定运行时将这些异步、低频且质量不均的观测转化为逐渲染帧的世界系对象锚点，并定义静止、运动、短时遮挡与重新获取期间的输出行为。
>
> 每个观测在时刻 (t_f) 采集、于 (t_a) 到达运行时，而锚点在渲染时刻 (t_r) 被应用消费。EgoAnchor按 (t_f) 恢复观测的世界坐标语义，并在每个 (t_r) 从已接纳的时间索引轨迹中合成输出。图~\ref{fig:arch} 展示了系统的数据流与主要运行时状态。

这样读者会自然留下三个印象：

* 无标记、无需逐物体训练；
* 类别不受平台预置集合限制，但要求三维模型；
* 输出面向稳定、连续与恢复，而不是只提供视觉位姿。

不需要显式概括成三个口号。

---

# 2. 三个时刻需要保留，但不要称为“贯穿两层的三条时间线”

(t_f,t_a,t_r) 是全文真正重要的概念，不能删除。尤其采集时刻语义是论文最清楚的系统贡献之一。

问题不在于三个时刻，而在于“贯穿两层的是三条不同的时间线”过于刻意。改成上面的自然陈述即可：

> 每个观测在 (t_f) 采集、于 (t_a) 到达，并在后续渲染时刻 (t_r) 参与锚点输出。

后续只在需要时使用：

* (t_f)：决定观测的空间语义；
* (t_a)：决定运行时何时收到结果；
* (t_r)：决定应用何时需要锚点。

不需要反复说“三条时间线”。

图2 caption 也可以缩短。当前 caption 几乎重新讲了一遍方法。建议控制为：

> EgoAnchor的分层架构。感知后端生成带采集时刻与VCD评分的相机系位姿候选；锚定运行时按采集时刻恢复世界系观测，经准入后写入时间索引轨迹，并在每个渲染帧根据运动与生命周期状态合成对象锚点。

---

# 3. “两条更新通路”不是论文核心，也不是两个串行步骤

根据手稿中的算法逻辑，当前实现不是“一条串行线”：

* 候选可能以约 9–13 Hz 异步到达；
* 渲染刷新以头显帧率发生；
* 两者在时间上交错执行；
* 它们共享轨迹、StaticLock 模式和生命周期状态。

因此，实现层面确实有两个事件处理过程，但不应称为“两个独立通路”，因为渲染过程会提交模式转换、初始化锁定位姿并更新接缝过渡状态，不是纯读取。

推荐的学术抽象是：

> **观测处理**与**锚点合成**。

建议将 §3.3 开头整体改成：

> 锚定运行时接收带采集时刻与可靠性评分的相机系位姿观测，并在每个渲染时刻输出世界系对象锚点。从运行时职责看，其处理分为观测处理与锚点合成。候选到达时，系统恢复采集时刻的世界系位姿、执行观测准入，并更新轨迹控制点、运动证据与生命周期状态；每个渲染帧，系统查询已有轨迹、提交待决状态转换，并根据运动模式与生命周期生成锚点位姿及其可信度。两类处理异步交错执行，并共享时间索引轨迹和运行时状态。

相应小标题建议改为：

* 3.3.1 **观测处理**
* 3.3.2 **锚点合成**
* 3.3.3 **静止锚定**
* 3.3.4 **生命周期**

当前“观测更新”和“逐帧输出”也可以接受，但“观测处理／锚点合成”更接近论文问题，而不是代码操作。

摘要、贡献和结论中不要出现“两条解耦更新通路”。那是实现组织，不是论文贡献。

---

# 4. 坐标与变换符号：改用常见的矩阵复合写法

当前第一句确实不够自然，而且“(\circ)”没有给读者带来价值。

建议改为：

> 本文用 (T_a^b\in\mathrm{SE}(3)) 表示将坐标从坐标系 (a) 变换至坐标系 (b) 的三维刚体变换，其中 (\mathrm{SE}(3)) 表示由三维旋转和平移组成的位姿变换。多个变换按矩阵乘法顺序复合。

随后将

[
T_o^w(t_f)
==========

T_c^w(t_f)\circ\Psi(T_o^{c_f})
]

改为更常见的：

[
T_o^w(t_f)
==========

T_c^w(t_f),\Psi!\left(T_o^{c_f}\right).
]

这样读者可以直接理解：

[
\text{object}\rightarrow\text{camera}\rightarrow\text{world}.
]

没有必要解释群论意义上的 (\mathrm{SE}(3))，一句“三维旋转和平移构成的刚体变换”已经足够。

---

# 5. 公式体系建议：减少两个公式，修正三个定义

目前公式整体顺序已经比早期版本清楚，但还可以进一步收束。

## 建议保留

* 观测元组；
* 双目深度；
* 零样本位姿估计；
* VCD深度评分与总评分；
* 历史查询时刻；
* 轨迹插值；
* Tracking/Locked输出；
* 锁内微调；
* CUSUM。

这些都对应明确的方法机制。

## 建议合并采集时刻对齐的两个公式

目前式(7)只是缓存查询，式(8)才得到世界位姿。可以合并为一个核心公式：

[
T_o^w(t_f)
==========

\mathcal H_c(f),
\Psi!\left(T_o^{c_f}\right).
]

正文解释：

> 运行时以帧标识 (f) 回查采集时刻的相机世界位姿 (\mathcal H_c(f)=T_c^w(t_f))，并与相机系对象位姿复合。

这一个公式已经完整表达采集时刻配准，减少了“时间对齐／空间对齐”被写成两个工程步骤的感觉。

## 建议删除观测集合公式

当前

[
\mathcal O^+={o_f\mid t_f>t_{\mathrm{latest}}\land R_f\ge R_{\min}}
]

后文没有继续使用 (\mathcal O^+)，只是把一个 `if` 条件包装成集合定义。建议删除公式，直接写：

> 当且仅当 (t_f>t_{\mathrm{latest}}) 且 (R_f\ge R_{\min}) 时，候选被接纳并写入轨迹。

这会提高阅读效率。

## VCD必须补齐空集合情况

当前文字说明两路模态可能同时失效，但式(6)在 (\mathcal K_f=\varnothing) 时仍会除零。

建议写成：

[
R_f =
\begin{cases}
V_f\exp!\left(
\dfrac{\sum_{m\in\mathcal K_f}w_m\ln\max(S_f^m,\epsilon)}
{\sum_{m\in\mathcal K_f}w_m}
\right),
& \mathcal K_f\neq\varnothing,[0.7em]
0,&\mathcal K_f=\varnothing.
\end{cases}
]

此外，当渲染投影完全位于视野外、(|M_{\mathrm{rnd}}|=0) 时，也应令 (R_f=0)。

## “端到端时延”应改名

当前 (\widehat\tau(t_r)) 是由最新控制点在渲染时的年龄估计，不是严格意义上的 capture-to-display 端到端时延。

建议使用：

* “观测年龄估计”；
* 或“候选时延估计”。

例如：

[
\widehat{\ell}(t_r)
]

并写为：

> 运行时跟踪最近可靠观测在渲染时刻的年龄 (\widehat{\ell}(t_r))。

如果实际代码跟踪的是 (t_a-t_f)，则应明确称为“采集至到达时延”。需要根据代码真实实现二选一，不能继续使用泛化的“端到端时延”。

## 统一 (i) 和 (j)

现在：

* (f)：图像帧；
* (i)：轨迹控制点；
* (j)：StaticLock接纳观测次序。

(i) 和 (j) 实际指向同一组接纳观测。建议统一使用 (i)：

[
\widehat T_i,\quad R_i,\quad \bar v_i,\quad E_{p,i}.
]

这样可以明显降低符号负担。

锁定位姿也建议写成 (T_{o,\mathrm{lock}}^w)，与 (T_o^w)、(\widehat T_o^w)、(\widetilde T_o^w) 保持一致。

---

# 6. 解锁条件应改成四类文字，不再使用大逻辑公式

你的注释是正确的。当前式(15)把不同单位、不同时间尺度和不同目的的条件塞进一个逻辑表达式，数学形式并没有提高理解。

建议删除解锁公式，仅保留 CUSUM 公式，然后写：

> StaticLock由四类证据释放：
>
> 1. **持续残差：**平移或旋转CUSUM超过门槛，表明死区外偏移持续存在；
> 2. **累计漂移：**观测共识相对入锁原点的平移或旋转偏移超过租绳范围，避免缓慢真实运动被长期吸收；
> 3. **明确运动：**估计速度持续高于运动门槛，提供早于累计位移的释放信号；
> 4. **可靠性下降：**候选评分持续低于释放门槛时主动解锁，避免保持可能错误的锁定位姿。

具体阈值全部留在附录。

这比大公式更学术，也更符合 StaticLock 的设计逻辑：它是一个多证据状态转换机制，而不是一条需要理论推导的判别方程。

---

# 7. Algorithmic Outline应保留，但压缩到约10步

当前算法仍然太接近代码，共二十余步，并重复了正文中的全部分支。VRGaussianAvatar的 Outline 有效，是因为它只帮助读者快速恢复方法流程，而不重新定义方法。

建议标题改为：

> **算法1：事件驱动的锚定运行时**

内容压缩为：

**候选到达时**

1. 根据帧标识回查采集时刻的相机世界位姿；
2. 将相机系候选恢复至世界坐标系；
3. 检查时间单调性与VCD准入条件；
4. 对接纳观测更新状态估计并追加轨迹控制点；
5. 更新StaticLock证据与生命周期计时。

**每个渲染帧**

6. 根据观测时延确定历史查询时刻；
7. 在已有控制点间插值，不进行运动外推；
8. 提交待决的入锁或解锁转换；
9. 根据Tracking/Locked状态确定锚点位姿；
10. 根据生命周期附加有效性与可信度。

算法中不必再次写 CUSUM、漂移租绳、速度逃逸等具体分支。正文已经定义，附录已有参数。

---

# 8. 生命周期目前仍然过于复杂，需按两个概念重新组织

目前正文同时出现：

* (R_{\min}=0.2)：轨迹准入；
* (R_{\mathrm{live}}=0.5)：刷新生命周期；
* 0.45持续600 ms：重新注册；
* 0.3持续600 ms：StaticLock释放；
* (R_{\mathrm{lock}}=0.25)：入锁。

这些参数可以真实存在于代码中，但正文不应逐一展开，否则 VCD 看起来像一组经验阈值系统。

正文只需建立两个概念：

### 轨迹准入

评分达到 (R_{\min}) 的观测可以更新轨迹。

### 追踪新鲜度

生命周期由“最近可靠观测”的年龄驱动；可靠性要求高于普通轨迹准入，因此低分观测可以用于有限的轨迹更新，但不能无限维持“正常追踪”状态。

建议将当前生命周期后半段压缩成：

> 生命周期由最近可靠观测的年龄驱动。评分达到准入门槛的候选可以更新轨迹，但只有达到更高可靠性要求的观测才刷新追踪新鲜度；因此持续低质量候选不会使系统无限维持正常状态。短时缺少可靠观测时，系统继续消费已有控制点；随后保持最后可信位姿并标记不确定性；持续失效则进入丢失并触发重新注册。

“持续低可靠性但仍有候选”是否额外触发重新注册，可以在最后补一句，不需要再详细解释两条退化路径。

---

# 9. 你没有注释、但仍需处理的全文问题

## “易部署”不宜作为核心特点

系统需要三维模型、外部GPU工作站和网络连接。“无需标记、无需逐物体训练”可以主张；“易部署”不够稳妥。

## Teaser中的五个物体需要明确性质

“正文只报告3个物体，覆盖范围由图承担，正文不解释”不可取。图像不能承担系统覆盖范围的证据。

可以展示五个物体，但 caption 应明确：

> 图中包括用户研究使用的三个物体及两个额外的定性演示物体。

不要让审稿人误以为五个物体均进入正式评价。

## 硬件部署描述仍有矛盾

正文说运行时运行于 Quest 3，又写“头显端主机配备 i9-14900K”。必须明确：

* i9主机是什么角色；
* Unity运行在Quest standalone还是PC；
* 相机图像、锚定运行时和最终渲染分别在哪个设备执行。

否则整个时延边界不明确。

## 控制器外参表述仍不够严谨

“固定偏置的残差是常量，因此不影响中心化指标”不一定普遍成立。物体坐标系中的固定平移残差会随物体旋转改变世界系误差方向。

更稳妥的写法是：

> 模型坐标系与控制器追踪坐标系之间的固定刚体变换在实验前标定，并在全部序列中保持不变。其残余误差包含在所报告的注册误差中。

同时说明标定方法，而不是只给出“相机系轴向平移 (-16) mm”。

## 统计实现细节可以下沉

“规范到12位小数、删除零差、枚举符号分配”等内容过于代码化。主文写：

> 使用带并列中秩的双侧精确Wilcoxon符号秩检验，并在预定义家族内进行Holm校正。

具体实现放补充材料。

## 讨论中的“三层证据作用域”仍略防御

可以改成主动综合：

> 受控评价与用户研究共同呈现出稳定优先的结果模式：StaticLock显著降低静止波动，参与者也报告更高的静止稳定、恢复一致、位置正确和依赖意愿；运动附着与姿态一致未检测到显著差异，与系统较长的起动转换和未一致改善的当前时刻误差相对应。

不需要再强调“实验三不是整个系统有效性的验证”。

## 结论不要再以“两条解耦通路”为中心

结论应回到：

> EgoAnchor通过VCD位姿验证、采集时刻世界配准、时间索引轨迹合成、运动状态感知的静止锚定与生命周期管理，把异步零样本位姿观测转化为稳定、可恢复的动态对象锚点。

这才是读者应记住的核心。

---

# 推荐修改顺序

第一步，重写 §3.1 与 §3.3 开头，删除“三项目标”和“两条独立通路”的强调。

第二步，统一公式体系：合并采集时刻配准，删除准入集合公式，补齐VCD空模态，统一 (i/j)，修正时延术语。

第三步，将解锁大公式改为四类文字证据，并压缩算法。

第四步，统一摘要、贡献和结论，全部服务于“异步零样本观测到稳定动态锚点”，不再突出接口、契约或并发组织。

第五步，处理部署设备、外参标定、非正式GPU结果和Teaser物体范围等提交级问题。

这次修改的核心不是继续解释运行时，而是让读者清楚记住：

> **EgoAnchor扩展可被零样本视觉定位的刚性日常物体，并通过明确的时间语义、质量准入、状态感知输出和生命周期，使这些视觉观测真正成为混合现实应用可以持续使用的对象锚点。**

你的理解更准确：**静止锚定应当被视为“逐帧锚点合成”的一部分，而不是与锚点合成并列的第三个处理阶段。**

关键在于区分两个层面：

* **实现调度层面**：StaticLock 的运动证据会在候选到达时更新，模式转换和最终位姿会在渲染帧提交。
* **方法功能层面**：StaticLock 决定每个渲染时刻最终输出什么锚点位姿，因此它属于锚点合成策略。

论文结构应优先反映第二个层面，而不是严格复制代码线程或事件回调。

---

## 1. 什么是“锚点合成”

我建议把“逐帧锚点合成”明确界定为：

> 给定已经接纳的世界系观测轨迹、当前运动状态以及渲染时刻 (t_r)，生成应用在该渲染帧实际消费的世界系锚点位姿。

在这个定义下，锚点合成至少包含三个环节：

1. 确定查询时刻 (t_q)；
2. 从时间索引轨迹中得到连续跟踪位姿；
3. 根据运动状态决定最终输出跟踪位姿、锁定位姿或二者之间的过渡结果。

因此：

[
\text{accepted observations}
\rightarrow
\text{tracking trajectory}
\rightarrow
\text{motion-aware output}
\rightarrow
\widetilde T_o^w(t_r).
]

StaticLock 显然位于第三步。

目前手稿把“逐帧输出”和“静止锚定”拆成两个平级小节，容易造成一种错误印象：

> 先完成逐帧输出，然后再额外执行静止锚定。

但从你们的最终输出方程看并不是这样。最终位姿本身就是：

[
\widetilde T_o^w(t_r)=
\begin{cases}
T_{o,\mathrm{lock}}^w, & m(t_r)=\mathsf{Locked},
\widehat T_o^w(t_q), & m(t_r)=\mathsf{Tracking}.
\end{cases}
]

也就是说，**轨迹取值只生成 Tracking 分支的候选输出，StaticLock 决定最终使用哪个分支。** 所以静止锚定不是合成后的附加滤波，而是合成函数内部的模式选择。

---

# 2. 更准确的概念层级

我认为全文应该形成下面的三级关系。

## 第一层：观测处理

回答：

> 哪些视觉候选能够进入锚点运行时，以及它们在世界坐标系中表示什么？

包含：

* 采集时刻配准；
* VCD 准入；
* 状态估计；
* 写入时间索引轨迹；
* 更新运动状态和 StaticLock 证据。

输出不是应用锚点，而是：

[
\left{
(t_i,\widehat T_o^w(t_i))
\right}
\quad+\quad
\text{runtime state}.
]

## 第二层：锚点合成

回答：

> 在每个渲染时刻，应用实际得到什么位姿？

包含：

* 历史查询时刻；
* Linear/SLERP 轨迹取值；
* Tracking/Locked 模式选择；
* 锁内微调；
* 入锁与解锁；
* 解锁后的接缝过渡。

输出是：

[
\widetilde T_o^w(t_r).
]

因此 StaticLock 属于这一层。

## 第三层：生命周期管理

回答：

> 当前输出是否仍然可信、是否应当保持、降级、隐藏或重新获取？

它不主要决定正常 Tracking/Locked 状态下的位姿数值，而是决定：

* 输出有效性；
* 不确定性；
* Holding/Lost 状态；
* 重新注册与恢复；
* 应用是否继续呈现附着内容。

所以生命周期最好与锚点合成分开。它处理的是**锚点的存在与可信状态**，StaticLock 处理的是**锚点位姿如何输出**。

---

# 3. 我建议的第3.3节结构

最清晰的结构不是当前的：

* 观测更新
* 逐帧输出
* 静止锚定
* 生命周期管理

而是：

## 3.3 锚定运行时

### 3.3.1 观测配准与准入

内容依次为：

* 采集时刻配准；
* 世界系复合；
* VCD准入；
* 状态估计与轨迹控制点。

这里最后补一句即可：

> 每个接纳观测同时更新运动状态及静止锚定所需的进入和释放证据。

不需要在这里展开 StaticLock。

### 3.3.2 逐帧锚点合成

这一节内部自然分成三个加粗段落：

**时间索引轨迹取值。**
定义 (t_q) 和 (\widehat T_o^w(t_q))。

**运动状态感知输出。**
定义 Tracking/Locked 两种输出模式，以及最终位姿方程。

**静止锁定与释放。**
解释进入锁定、锁内微调、解锁证据和接缝过渡。

这样 StaticLock 的全部内容仍然保留，但概念归属更准确。

### 3.3.3 生命周期管理

解释：

* Tracking；
* Buffered/Holding；
* FrozenUncertain；
* Lost；
* Reacquisition；
* 输出可信度。

### 3.4 运行时流程

Algorithmic Outline 总结两类事件：

* 候选到达；
* 渲染刷新。

---

# 4. 为什么这种结构比“两个更新通路”更适合论文

“两类事件”是事实，但不是论文的核心方法分解。

如果按代码执行方式组织论文，就会出现你现在遇到的问题：

* StaticLock 证据在候选到达时更新；
* StaticLock 位姿在渲染时输出；
* 入锁请求由一侧产生、另一侧提交；
* 生命周期计时由观测更新、状态表现由渲染输出。

于是任何完整机制都横跨两类事件，很难归入“观测通路”或“渲染通路”。这正说明“两个通路”只适合作为 Algorithmic Outline 的调度说明，不适合作为方法章节的主要分类。

按功能组织后则非常自然：

[
\boxed{\text{观测处理}}
\rightarrow
\boxed{\text{锚点合成}}
\rightarrow
\boxed{\text{生命周期管理}}
]

其中：

* 观测处理建立可信的世界系轨迹；
* 锚点合成生成逐帧位姿；
* 生命周期决定该位姿当前以何种可信状态交付应用。

代码中它们可以异步交错，论文中不需要因此打乱概念层级。

---

# 5. “静止锚定”在锚点合成中的精确位置

还可以进一步区分：

## 轨迹合成不是完整的锚点合成

Linear/SLERP 得到的是：

[
\widehat T_o^w(t_q),
]

它只是一个连续的**跟踪轨迹位姿**。

它尚未考虑：

* 物体是否已静止；
* 是否应保持锁定位姿；
* 是否正在解锁过渡；
* 当前生命周期是否允许正常交付。

真正的锚点输出是：

[
\widetilde T_o^w(t_r)
=====================

\mathcal S!\left(
\widehat T_o^w(t_q),
T_{o,\mathrm{lock}}^w,
m(t_r)
\right),
]

其中 (\mathcal S) 表示运动状态感知的输出策略。

因此可以建立很清楚的术语层级：

* **轨迹取值 / trajectory sampling**：得到连续跟踪位姿；
* **锚点合成 / anchor synthesis**：结合 Tracking/Locked 状态得到最终位姿；
* **锚点交付 / anchor delivery**：结合生命周期提供位姿与可信度。

这一层级会让公式含义明显更清楚。

---

# 6. StaticLock的进入与解锁证据是否也属于锚点合成？

属于，但要区分“状态更新发生在哪里”和“机制属于什么”。

例如 CUSUM 是在新观测到达时计算的，但它存在的唯一目的，是决定未来的锚点输出模式。它不是观测质量验证，也不是轨迹估计本身。

所以在方法分类上，它属于 StaticLock／锚点合成；在 Algorithm 中，它列在“候选到达时更新证据”。

可以用一句话解决这种表面矛盾：

> 静止锚定是逐帧锚点合成中的运动状态感知输出策略。其运动证据随接纳观测更新，而模式转换和最终输出在渲染帧边界提交。

这句话已经准确表达了你们实现，不需要发明“跨轴状态”。

---

# 7. 是否还需要单独保留“静止锚定”小标题

有两个可行方案。

## 方案A：作为3.3.2内部加粗段落

这是我更推荐的：

### 3.3.2 逐帧锚点合成

* **轨迹取值。**
* **静止锚定。**
* **模式转换。**

优点是概念层级最干净，也减少小节数量。

## 方案B：保留三级小节

如果 StaticLock 公式和文字确实较长，可以写：

### 3.3.2 逐帧锚点合成

#### 3.3.2.1 时间索引轨迹

#### 3.3.2.2 静止锚定

但 IEEE VR 双栏论文通常不宜出现过深标题层级。你们可以通过加粗行内标题达到同样效果。

不建议继续让“逐帧输出”和“静止锚定”成为同一级别的两个独立过程。

---

# 8. 生命周期是否也是锚点合成的一部分？

广义上可以说是，但论文中最好不要这样归类。

原因是生命周期主要改变的是：

[
(\widetilde T_o^w(t_r),,q(t_r)),
]

其中 (q(t_r)) 是锚点的有效性或可信状态。

StaticLock主要决定第一项——位姿；生命周期主要决定第二项——输出状态，并在 Holding/Lost 时约束第一项是否继续沿用。

因此最清楚的表达是：

> 逐帧锚点合成确定“输出在哪里”，生命周期管理确定“该输出当前是否以及如何被应用使用”。

两者都属于锚定运行时，但功能不同。

---

# 9. 对摘要和贡献的影响

采用这一结构后，摘要可以改成：

> 锚定运行时通过采集时刻世界配准和VCD准入建立可信的时间索引轨迹，并结合历史轨迹取值与运动状态感知的静止锚定合成逐帧位姿，由分级生命周期管理短时失效与恢复。

这里层级非常清楚：

1. 建立轨迹；
2. 合成位姿；
3. 管理生命周期。

贡献2也可以写成：

> **VCD位姿验证与状态感知锚定运行时：**VCD依据可视度、颜色和深度一致性限制错误候选进入轨迹；锚定运行时按采集时刻恢复世界坐标语义，并通过时间索引轨迹取值和运动状态感知的静止锚定生成逐帧对象位姿，由分级生命周期定义遮挡、失效与恢复期间的交付行为。

比当前把所有机制平铺在一句中更有结构。

---

## 结论

你的判断成立，而且它解决了当前第3.3节最根本的分类问题：

> **静止锚定不是位于“逐帧输出”之后的独立模块，而是逐帧锚点合成中的状态感知输出策略。**

最终推荐的概念结构是：

[
\boxed{
\text{观测配准与准入}
\rightarrow
\text{逐帧锚点合成}
\rightarrow
\text{生命周期管理}
}
]

其中逐帧锚点合成包括：

[
\boxed{
\text{历史轨迹取值}
+
\text{Tracking/Locked模式选择}
+
\text{静止锁定、释放与过渡}
}
]

“两类事件驱动处理”只需要在运行时流程和算法中交代，不应承担第3章的主要方法分类。
