这次我按你的新要求重新处理了一遍，重点不是继续删内容，而是**重新控制“文字—公式—符号”的比例**。当前稿件里，你标出的几个问题确实成立：方法已经比之前学术化，但符号仍有“为描述一个工程动作而临时造一个记号”的倾向，而且若干关键关系还埋在行内文字中。比如当前感知流水线同时使用 `Detect`、`Seg`、`Stereo`、(\Phi) 四套不同风格的算子；观测校正之后又突然集中解释 (T,\widehat T,\widetilde T)；查询时刻没有明确区分“帧索引”和“时间索引”；StaticLock一段中连续出现大量阈值和临时符号。

VRGA真正值得借鉴的正是这一点：它不会为了每个已有组件都发明一套函数符号。其IK solver主要通过文字说明；真正需要表达系统几何关系时，才连续使用 (T_H^L,T_H^R,T_W^e,T_{W,e}^C) 等关键行间公式。到了核心模型，它再使用 `Decoder`、`LBS`、`Render` 这类具有明确计算意义、且在后文继续参与推导的算子。  这比“每一个模块都函数化”更适合EgoAnchor。

我已经据此生成下一版：

[下载 v12 学术符号与公式精修版](sandbox:/mnt/data/egoanchor_cn_final_v1_v12_notation_polished.tex)

这版主要做了以下调整。

### 1. 感知流水线不再使用 `Detect / Seg / Stereo / Φ` 四套临时符号

这是你这轮最重要的注释之一。

我没有改成 (F_{\mathrm{Detect}})、(F_{\mathrm{Seg}}) 或 `MLP_Detect`。那样只是把不必要的符号换了一种写法，而且 `MLP` 在数学上也不符合实际模块结构。

新的处理是：

* 检测和时序分割：**直接用文字描述**，因为对应算子后文不参与任何推导；
* 立体几何：只保留真正影响后续尺度关系的
  [
  Z_f=\frac{bf_x}{d_f};
  ]
* 位姿阶段：使用具有明确语义的
  [
  \operatorname{Reg}(\cdot),\qquad \operatorname{Track}(\cdot),
  ]
  而不是再为FoundationPose定义一个抽象的 (\Phi)。

也就是：

> 一个符号如果只出现一次、之后没有任何数学作用，就没有必要为了“显得学术”而创造它。

这与VRGA处理off-the-shelf IK模块和真正数学操作的方式更一致。

---

### 2. 全文符号约定移到了系统概览

你的批注“这种规定应该靠前统一文章叙述”是对的。

原稿直到观测校正后才告诉读者：

* (T_o^w) 是单次观测；
* (\widehat T_o^w) 是平滑轨迹；
* (\widetilde T_o^w) 是最终锚点。

现在在 §3.1 一开始统一定义：

* (T_a^b)：从 (a) 到 (b) 的刚体变换；
* (T_o^w)：世界系观测；
* (\widehat T_o^w)：轨迹估计；
* (\widetilde T_o^w)：最终锚点。

后文不再重复解释。

这是比较典型的VRGA式写法：先建立符号语义，后面的公式直接使用。

---

### 3. VCD改成更集中的三组公式

原稿VCD的数学本身没有大问题，但文字和公式彼此穿插较多。

现在集中成：

**投影支持：**

[
\Omega_f=M_f\cap M_f^{\mathrm{rnd}},\qquad
\Omega_f^\circ=\operatorname{Erode}(\Omega_f),\qquad
V_f=\frac{|\Omega_f|}{|M_f^{\mathrm{rnd}}|}.
]

一条公式同时定义：

* 共同支撑区；
* 腐蚀核心区；
* 可视度。

不用再分别在三句话里塞行内公式。

**深度融合：**

[
S_f^D
=====

(1-\alpha_f)S_f^{D,\mathrm{abs}}
+
\alpha_f S_f^{D,\mathrm{str}}.
]

按你的上一轮要求，不再恢复那个很长的absolute-depth公式，只用两句话定义绝对项和结构项的来源。

**最终VCD：**

现在直接用分段形式把 (\mathcal K_f=\varnothing) 写进定义：

[
R_f=V_f
\begin{cases}
\exp(\cdots),&\mathcal K_f\neq\varnothing,
1,&\mathcal K_f=\varnothing.
\end{cases}
]

这样删除了后面一大串“如果只有深度如何、两路都没有如何”的文字解释，同时公式定义域完整。

也删掉了：

* “正确位姿不会因此被误拒”；
* “错误位姿仍会被拦截”；
* “使重新获取成为可能”；

这些都不是VCD定义本身需要承担的论证。

---

### 4. 观测校正的数学链条现在只有三步

原稿这里同时有时间语义、缓存、状态变量、Kalman内部状态、帽号解释等信息，比较散。

现在收束为：

#### 世界系恢复

[
T_o^w(t_f)
==========

T_c^w(t_f),
S T_o^{c_f}S^{-1}.
]

#### 准入

如果当前已有 (j) 个接纳观测，新候选满足：

[
t_f>t_j,\qquad R_f\ge R_{\min}.
]

我特意没有再引入一个只使用一次的 (a_f) 或 (t_{\mathrm{latest}})。

#### 时间索引轨迹

[
\mathcal B_j
============

{(t_i,\widehat T_i)}_{i=1}^{j}.
]

这里也删除了方法正文中的：

* ((\mathbf p_j,\mathbf v_j))；
* ((\boldsymbol\delta_j,\boldsymbol\omega_j))。

因为这些Kalman内部状态后面实际上不参与方法推导。正文只需要知道：

> 接纳观测经过状态估计成为 (\widehat T_j)，进而构成 (\mathcal B_j)。

具体位置/旋转滤波器结构已经在系统实现中说明，没必要在方法里重复制造符号。

---

### 5. 彻底澄清了你对“查询时刻”的疑问

你问得很关键：

> “查询时刻不是按照帧编号查询的吗？”

答案是：**这两个地方的索引不同。**

现在正文明确写：

> 帧标识 (f) 只负责将视觉观测与采集时刻的相机位姿配对；一旦观测被接纳进入轨迹，轨迹使用采集时间 (t_i) 建索引，因此锚点合成查询的是轨迹上的目标时间 (t_q)，不是某一个图像帧编号。

这解决了原稿最容易混淆的时间语义。

随后公式压缩为：

[
\Delta_r
========

\max(\Delta_{\min},s\widehat\ell_r),
\qquad
t_q=t_r-\Delta_r,
]

其中 (\widehat\ell_r) 明确叫：

> 最新控制点年龄 (t_r-t_j) 的平滑估计。

不再使用函数式的：

[
\ell(t_r),\quad \bar\ell(t_r),\quad \Delta(t_r)
]

三套写法，也不再误称“端到端时延”。

---

### 6. 轨迹取值公式也压缩了

旧版piecewise公式包含：

* 区间内插值；
* 超过末端；
* 小于首端。

这很像程序边界判断。

现在只把真正的方法关系放进公式：

[
\lambda_q
=========

\frac-t_i},
\qquad
\widehat T_o^w(t_q)
===================

\operatorname{Interp}
(\widehat T_i,\widehat T_{i+1};\lambda_q).
]

然后一句话：

> 查询超出轨迹两端时保持最近端点，不进行速度外推。

这就是你所说的“公式关键到位，而不是大段文字解释”。

---

### 7. 输出模式从 (m(t_r)) 改成了 (\sigma_r)

你的直觉是合理的。

(m) 本身不算错误，hybrid system里也常用mode (m)，但本文已经有大量 (M) 表示mask，视觉章节也频繁出现 (M_f)。从视觉阅读上并不理想。

我改成：

[
\sigma_r
\in
{\mathsf{Tracking},\mathsf{Locked}},
]

并定义：

[
\widetilde T_o^w(t_r)
=====================

\begin{cases}
\widehat T_o^w(t_q),&\sigma_r=\mathsf{Tracking},
T_{o,\mathrm{lock}}^w,&\sigma_r=\mathsf{Locked}.
\end{cases}
]

(\sigma)作为离散切换状态比 `pattern` 更规范，也不会和mask混淆。

同时锁定位姿统一成：

[
T_{o,\mathrm{lock}}^w,
]

锚定参考统一成：

[
T_{o,\mathrm{org}}^w.
]

这样和 (T_o^w) 的对象下标体系保持一致。

---

### 8. 生命周期已经真正弱化

你之前一直要求这一点，原稿虽然去掉了独立subsection，但仍然有一整段状态机描述，包括 `FrozenUncertain` 和 `Lost`。

这一版只保留一句系统语义：

> 可靠观测短时中断时继续使用已有轨迹并在其耗尽后保持最后可信锚点；持续失效标记不可用，后续可靠观测通过准入后恢复输出。

不再在方法章节列代码状态名和多级阈值。

实验部分需要 `FrozenUncertain` 做操纵检验时仍然可以使用它，不影响方法主线。

---

### 9. StaticLock去掉了One-Euro比较

你标出的：

> “这里没有必要踩One-Euro吧？”

我同意。

One-Euro是评价baseline，不需要在方法定义StaticLock时出现。

现在StaticLock直接开始：

> StaticLock将静止状态从连续轨迹中显式分离，通过驻留进入、死区保持和多证据释放抑制静止期残余波动，同时保留对真实运动的响应。

没有比较、没有辩护。

---

### 10. StaticLock中大量行内条件已转为行间公式

原来的“进入锁定”一段有：

* (\bar v_j)
* (\bar\omega_j)
* (v_{\mathrm{th}})
* (\omega_{\mathrm{th}})
* (R_j)
* (R_{\mathrm{lock}})
* (\tau_{\mathrm{dwell}})
* (T_{\mathrm{lock}})
* (T_{\mathrm{org}})

全部挤在一句中文里。

现在核心进入条件独立成：

[
\bar v_j\le v_{\mathrm{th}},
\qquad
\bar\omega_j\le\omega_{\mathrm{th}},
\qquad
R_j\ge R_{\mathrm{lock}}.
]

正文只说明这些条件需要持续 (\tau_{\mathrm{dwell}})。

锁内更新也删除了仅用于包装条件的 (\mathcal D_j)，直接写：

[
T_{o,\mathrm{lock},j}^w=
\begin{cases}
\operatorname{Interp}
(T_{o,\mathrm{lock},j-1}^w,\widehat T_j;g_j),
&
d_{p,j}\le d_{\mathrm{db}},
\ d_{\theta,j}\le\theta_{\mathrm{db}},

T_{o,\mathrm{lock},j-1}^w,
&\text{otherwise}.
\end{cases}
]

也就是说，删除了一个后文完全不会再使用的符号 (\mathcal D_j)。

---

### 11. CUSUM又删除了一个临时符号 (\kappa_j)

原稿先写：

[
\kappa_j=\Delta t_j/t_{\mathrm{ref}}
]

然后再把 (\kappa_j) 放进CUSUM，这个记号只使用一次。

现在直接写成：

[
E_
==

2^{-\Delta t_j/h_E}E_{p,j-1}
+
\frac{\Delta t_j}{t_{\mathrm{ref}}}
R_j
[d_{p,j}-d_{\mathrm{db}}]_+.
]

少一个符号，数学意义反而更直接。

这是我认为全文应该继续遵循的原则：

> **不要为一个简单代数因子单独造名字，除非它后面还会被反复使用。**

---

### 12. 四类释放证据按你的要求改成整齐列表

当前长句确实难读。

现在变为四个非常短的条目：

* **持续残差**
* **累计漂移**
* **明确运动**
* **可靠性下降**

列表只解释每一路证据检测什么，不重新列全部持续时间和阈值。

具体阈值仍然在附录。

---

## 我现在对全文公式体系的建议

经过这一轮，方法里的公式可以理解成五组，而不是十几个零散公式：

**感知形成**

[
Z_f
\rightarrow
T_o^{c_f}
]

**VCD**

[
(\Omega_f,V_f)
\rightarrow
S_f^D
\rightarrow
R_f
]

**观测校正**

[
T_o^{c_f}
\rightarrow
T_o^w(t_f)
\rightarrow
\mathcal B_j
]

**锚点合成**

[
t_q
\rightarrow
\widehat T_o^w(t_q)
\rightarrow
\widetilde T_o^w(t_r)
]

**StaticLock**

[
\text{entry}
\rightarrow
T_{o,\mathrm{lock}}^w
\rightarrow
E_{p,j}
\rightarrow
\text{release}.
]

这个层级已经比较接近VRGA第3–4页的阅读节奏：关键状态用公式串起来，公式之间只留下真正需要解释的设计理由。VRGA在页面视觉上也是连续的公式块与短解释段交替，而不是大量零散的行内数学。

我同时做了结构检查：新稿没有重复label或括号失配；当前检测到的两个未解析引用仍然只是外部输入表 `tab:exp1-performance` 和 `tab:exp3-subjective`，不是这轮修改造成的。
