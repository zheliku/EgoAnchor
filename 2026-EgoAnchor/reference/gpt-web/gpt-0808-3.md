我按你这轮的所有注释重新处理了第5章，并同步改了实验二结果中 AURC 的解释位置。新版在这里：

[下载修改后的 `egoanchor_cn_final_v2(5)_eval_restructured.tex`](sandbox:/mnt/data/egoanchor_cn_final_v2%285%29_eval_restructured.tex)

这次我不是逐句打补丁，而是重新调整了第5章的**篇幅重心、视觉层级和实验叙事**。你当前版本明确要求实验一/二/三大约按 3:2:5 分配，同时指出列表过多、实验一过细、实验二应压缩、用户研究需要显著加强。

我认为这个方向是对的。VRGA 的实验章节也不是把各实验平均铺开：其用户研究明显占据主要篇幅，而且采用 `Study Design and Task → Dependent Variables → Procedure → Participants` 这种职责非常明确的组织；量表为什么选、测量什么，也会认真展开。

### 这次最重要的调整

**实验一压缩到“系统行为基准”的职责。** 我删除了平台预测/共同漂移以及 `-16 mm` 固定补偿等解释。这些内容在 Discussion 的 limitation 中已经有位置，不应该堵在实验设计开头。场景也不再五个 bullet 平铺，而把持续平移和持续旋转合并成“持续运动”这一概念类别，因此现在是四类典型行为、五组实际采集序列。

Baseline 也不再写成配置文件式差异。现在的关系是：

> Arrival：朴素异步消费
> Capture：恢复采集时刻语义
> One-Euro：主要强平滑基线
> EgoAnchor：完整系统

One-Euro被明确介绍为成熟、轻量、广泛用于实时交互输入的速度自适应滤波方法，因此作为 stability–responsiveness trade-off 的主要对照。没有再写“其余组件都完全一样所以我们的比较很公平”这类申诉式语言。

**实验一指标也大幅去工程化。** 你指出的：

```latex
[0,600] ms 内以 5 ms 步长搜索
```

已经删除。现在只解释每个指标在论文中回答什么：

* 世界一致性；
* 静止稳定性；
* 动态保真度；
* 遮挡鲁棒性；
* 转换代价。

同时补了一句真正有用的选择依据：对象锚点在应用中需要分别应对观察者运动、静止、物体运动、遮挡和起停转换，因此五组指标直接对应这些行为。原稿的指标定义确实塞入了过多计算过程。

### 实验二现在刻意保持短

我没有继续给实验二制造子小节，而是用四个加粗段落：

```latex
\textbf{采集时刻对齐。}
\textbf{StaticLock。}
\textbf{VCD。}
\textbf{轨迹输出策略。}
```

这样四组归因与 Figure 3 四个 panel 是平行的，但不会出现四层小标题。

你特别指出要把“为什么比较外推和插值”与Related Work串起来。这次改成：

> 实时跟踪通常可通过预测外推降低响应滞后，而EgoAnchor采用只消费已到达控制点的历史取值。因此比较 Smoothed KF Extrapolation 与历史插值。

随后只用一句定义 Smoothed 的含义：

> 从最新卡尔曼平滑状态按常速度模型外推到当前渲染时刻。

这样它不再突然出现，同时和Related Work里已经讨论过的预测式跟踪自然呼应。

### AURC的详细解释移到了结果

你问“这些是不是应该放结果”，我的判断是：**完整解释确实更适合结果，但Evaluation Design仍必须告诉读者AURC是什么。**

所以第5章现在只留下：

> VCD以风险–覆盖率曲线下面积AURC评价排序质量，越低越好。

第6章真正第一次读图时再解释：

> 按VCD从高到低扩大coverage → 保留候选平均平移误差作为risk → 曲线下面积为AURC。

然后马上接：

> 4.79 vs 7.25 mm，12/12遮挡过程均有正向排序收益。

这样“定义”和“读结果”发生在最合适的位置，不再让实验二的方法段承担一整段指标教程。

---

## 用户研究这次是真正的正文重点

这是这轮最大的重构。

我改成了VRGA更接近的流动式结构，不再堆一排 `\subsubsection`：

```text
实验三：用户研究

实验设计。
任务。
主观测量。
实验流程。
参与者。
统计分析。
```

这是我认为比上一版明显更好的地方。VRGA也是在一个User Study节中用粗体段落依次定义设计、因变量、Procedure和Participants，而不是每一个操作细节都升成编号标题。

### “13项”这个歧义已经彻底删除

你说得对。论文分析的不是“13项结局”。

根据你上传的原始实验表，我重新核对了问卷结构：

* 7项自定义锚定条目；
* AQ有6道题，但形成 **2个子量表得分**；
* TiA形成 **2个分量表得分**；
* S-TIAS形成 **1个得分**。

所以真正进入主要统计分析的是：

[
7+2+2+1=12
]

项主观结局。

新版正文不再说“十三项七点评分”。而是明确讲各个测量工具，并在统计分析最后统一说明**形成12项主观结局**。这也和第6章“完整的12项结局”完全闭合。

### 七项定制条目被提升为研究设计的一部分

我保留了七项列表，因为这里恰恰值得列表化，而不是把所有地方的列表都删掉。

现在先建立设计逻辑：

> 定制条目结合实验一的应用侧评价维度，以及AR配准误差与用户信任研究，覆盖动态对象锚定在静止、运动、旋转、遮挡恢复、空间正确性以及稳定–响应权衡中的直接感知。

然后才列：

* 静止稳定；
* 运动附着；
* 姿态一致；
* 恢复一致；
* 位置正确；
* 依赖意愿；
* 稳定–响应平衡。

这样它们看起来是**有系统依据地设计出的anchor-specific measures**，而不是临时凑的七道题。

---

## AQ / TiA / S-TIAS也重新认真写了

这里我特别核对了你上传的原始问卷表，而不是只根据旧稿改措辞。

### AQ

现在明确说明：

> Augmentation Quality面向XR增强质量。

你们采用的是完整的两个三条目子量表：

* **Embedding Quality**：融合、嵌入、尺度协调；
* **Interaction Quality**：交互直观、实时响应、运动平滑。

因此它与七个anchor-specific items的关系非常清楚：

> 七项定制条目看“对象锚定本身”；AQ看“这个锚定最终形成的XR augmentation quality”。

这会显得研究设计完整得多。

### TiA

我没有把TiA写成“信心量表”，因为从学术概念上讲 **Trust in Automation** 更准确。

现在明确说明使用：

* Reliability/Competence：6项；
* Understanding/Predictability：4项。

前者测能力与可靠性判断，后者测理解与可预测性。

这与原始数据表完全一致。

### S-TIAS

这里我没有直接写成“AI结果量表”，因为这个说法容易被审稿人抓概念。

但你的核心想法被保留了，而且表达得更严谨：

> S-TIAS原始三项直接评价AI assistant的 confidence、reliability 和 trust；EgoAnchor的对象观测由学习式视觉模型产生，因此本文将评价对象适配为对象锚定方法，用来补充更直接的总体信任判断。

这样既把**为什么在TiA之外还需要S-TIAS**说清楚，也没有把EgoAnchor夸张地描述成“所有输出都是AI生成内容”。

---

## Participants部分我直接根据原始数据重新核对了

新版现在包括：

* (N=24)，全部完成并纳入；
* 年龄20–36，(M=27.08,\ SD=4.45)；
* 12女性 / 12男性；
* 22右利手 / 2左利手；
* 15正常视力 / 9矫正后正常；
* VR/MR使用经验分布；
* 实物AR/MR交互经验分布。

这比原来只有性别、年龄和惯用手完整得多，也更接近VRGA Participants部分会报告人口统计和相关经验的方式。VRGA同样专门报告年龄、性别和相关AR/VR经验。

这里我留了**唯一一个内部NOTE**：

```latex
% NOTE: 当前原始数据未记录参与者的具体招募渠道，
% 因此正文未推测招募来源；如有真实记录，可在此补充。
```

因为我检查了你上传的 `Participants` 表，它有完整人口统计和经验数据，但**没有“通过什么渠道招募”这一字段**。VRGA可以写“through the campus website”，因为那是他们自己的实验事实。 我们不能照抄。如果你告诉我你们实际是“校内邮件/实验室招募平台/社交媒体/校园公告”等，我再补进去即可。

---

## 统计分析也重新整理了

现在读者能明确知道最终数据是如何从原始回答变成论文里的12个结果：

> 七项自定义 + AQ两子量表：参与者内先跨三个物体聚合；
> TiA两分量表 + S-TIAS：直接使用方法级得分。

然后统一报告：

> median [Q1,Q3]、配对差异、Wilcoxon、matched rank-biserial correlation、participant-level bootstrap interval。

以及两个预设Holm family：

[
7\text{项定制条目}
]

和

[
AQ\text{-EQ},\ AQ\text{-IQ},
TiA_{\mathrm{RC}},
TiA_{\mathrm{UP}},
S\text{-TIAS}.
]

这与第6章当前结果组织完全一致。

---

另外，这次没有再把“训练用耳机盒”写进论文。实验流程只说：

> “通过一次练习熟悉任务。”

这是论文需要知道的信息。至于练习到底拿了哪个备用物体，没有研究意义。

我也做了基础TeX结构检查：新文件花括号闭合、`enumerate/itemize`环境闭合、label无重复。由于当前上传的是单独主TeX，没有它引用的外部表格与图片资源，因此我没有把“完整latexmk通过”作为本轮判断依据。

我按你这轮注释重新处理了一遍，而且这次不是只改第5章局部句子，而是把你指出的几类问题在**全文相关位置同步检查**了。

[下载修改后的完整 TeX：egoanchor_cn_final_v2(6)_ch5_polished.tex](sandbox:/mnt/data/egoanchor_cn_final_v2%286%29_ch5_polished.tex)

这轮我主要做了下面几件事。

* **术语统一为“机制归因”**。我最终不建议用“组件消融”。实验二的四项里，StaticLock属于典型消融，但VCD评价的是评分排序能力，轨迹输出又是两种策略的比较，并不都是“remove-one-component ablation”。因此全文统一成“机制归因”，摘要、实验二标题、结果标题、图注、贡献和结论都同步了。原稿这里确实混用了“组件归因”和“关键机制归因”。
* **第5章的中英文混排重新统一**。指标现在首次出现时采用“中文术语（英文术语）”，后文直接用中文。例如“时延对齐均方根误差（lag-aligned RMSE）”“当前时刻均方根误差（current-time RMSE）”“残余抖动（residual jitter）”“起动转换时延（Start-transition）”。结果和讨论里的 `capture-time / arrival-time / residual jitter / Smoothed KF Extrapolation` 也同步改成中文叙述；One-Euro、StaticLock、VCD、AQ、TiA、S-TIAS这类专有名称保留。
* **实验三的物体选择重新写了**。不再用“使结论不局限于单一有利目标”这种带辩护感的句子，而是明确给出物体选择的四个维度：**尺度、几何形态、表面纹理、抓握遮挡**。然后分别解释鼠标、订书机和手柄在这些维度上的差异，最后定义三者构成一个异质测试集合。这比泛泛说“覆盖不同日常物体”更有说服力，同时又不会暗示三个物体能够代表所有日常物体。你原来的批注正是在要求这一点。
* **遮挡任务删除了工程状态名称**。正文现在只描述“统一挡板、0.6--0.9 s完全遮挡、随后观察恢复”，预实验的作用也只写成“形成可重复的短时完全遮挡过程”。`FrozenUncertain` 和 `Lost` 不再出现在用户研究protocol中。更进一步，我全文检查后把方法和结果里暴露的这两个代码状态名也去掉了，统一用“不确定保持”“锚点丢失”等论文语义。原稿这里确实把内部实现状态直接带进了实验任务。
* **“实验员”也删除了**。谁拿挡板不是研究问题，真正需要保留的是遮挡工具和遮挡时长的一致性。
* **所有类似“避免打断体验”的防御性解释删除了**。任务现在直接收束成：“每个方法--物体条件完成三项任务后进行一次统一评分。”没有再解释为什么不在中途打断。你指出的这一类写法在当前稿件中比较典型。

### 主观测量这一节基本重写了

这一部分我主要参考了VRGA的写法。VRGA不会先说“没有现成量表，所以我们不得不……”，而是直接定义研究要测的构念，然后介绍采用了哪些已有量表、各量表包含哪些维度以及为什么与研究问题相关。 它随后再独立描述Procedure和Participants。

因此现在开头直接变成：

> 我们将对象锚定的感知质量与信任作为主要主观测量。条件级测量由七项对象锚定条目和增强质量量表组成；全部正式条件完成后，再使用两套信任量表评价参与者对两种方法的整体判断。

然后依次介绍：

**对象锚定条目。** 明确说明七项设计来自实验一的应用侧维度，并参考AR配准误差与信任研究。七项仍然逐项列出，因为这七项本身就是用户研究的重要设计内容，不应该再压成一句。

**增强质量（Augmentation Quality, AQ）。** 现在先给中文概念，再说明采用嵌入质量（Embedding Quality）和交互质量（Interaction Quality）两个完整三条目子量表，以及它们分别测什么。删掉了“与自定义条目形成明确分工”“保留完整子量表以维持内部结构”之类刻意解释。

**自动化信任（Trust in Automation, TiA）。** 明确介绍可靠性/能力和理解/可预测性两个分量表，各自对应的心理构念。不再写“锚点是否被采纳取决于使用者是否信任它”这种动机句。

**S-TIAS。** 保留了你希望突出的一点：EgoAnchor依赖学习式视觉模型形成对象观测，因此S-TIAS用来补充用户对智能系统输出整体信心、可靠性和信任的评价。但我没有把它夸张成“AI生成内容量表”，避免概念上过界。

整个组织现在更接近VRGA的Dependent Variables：**先确定测量构念，再介绍量表本身**，而不是写成一段量表选择申诉。VRGA在User Study中也是直接解释VEQ、VEQ+和VHPQ分别测什么，并说明所采用的子量表。

### 统计分析也按你的注释修了

原稿的：

```latex
$6-\mathrm{raw}$
```

已经删除。现在写成：

> TiA中的反向条目按照原量表计分规则完成反向计分后再计算分量表得分。

原来的：

```text
median [Q1, Q3]
```

也不再直接扔给读者，现在第一次规范定义为：

> 中位数及第一、第三四分位数（Mdn [Q1, Q3]）

统计分析逻辑现在是：

> 7个对象锚定条目 + AQ两个子量表 + TiA两个分量表 + S-TIAS = 12项主观结局；
> 对象锚定条目和AQ先对三个物体做参与者内聚合；
> TiA和S-TIAS使用方法级得分；
> Wilcoxon做方法内配对比较；
> 报告匹配秩双列相关和参与者级自举区间；
> 两个测量家族分别进行Holm校正；
> 已发表量表报告Cronbach's (\alpha)。

比原先把计分公式、并列秩、`raw`等实现细节堆在一起更像论文统计方法。

### 我还顺着你的批注检查了第6章和讨论

这一步比较重要，因为否则第5章改干净以后，第6章又会把旧术语带回来。

例如原结果还同时使用 `lag-aligned RMSE`、`current-time RMSE`、`residual jitter`，以及 `capture-time / arrival-time`。  现在都已经和第5章统一。

实验二结果也从：

> “组件归因”

同步为：

> “机制归因”。

Figure 3图注也同步。

此外我删掉了结果里一句：

> “该量比较的是评分诱导的排序质量，而非某个冻结阈值下的接纳性能。”

它属于你反复指出的那种预防审稿人误解式写法。第5章已经定义AURC是排序质量，结果直接报告数据即可。

讨论里也做了同类清理。例如原来的：

> “两者均不等价于……帧对齐也不是……”

改成了正向定义：

> 有效时延、时延对齐残差与当前时刻配准误差分别刻画响应滞后、轨迹保真度和实时配准表现。

原来的“**三层证据的一致性与作用域**”也改成了更自然的“**客观与主观证据**”，并删除最后一段解释实验三“只能说明什么”的防御性限定。

### 关于VRGA，这一轮我认为应该学习的是它的实验叙事节奏

它在第5页先直接定义实验因素和条件，然后解释task；接下来进入Dependent Variables，认真说明为什么使用哪些构念和量表；第6页再依次给Procedure和Participants。

所以我们的第5章现在也更接近：

> **实验一：系统表现是什么 → 实验二：关键机制分别贡献什么 → 实验三：用户实际如何感知这些表现**

其中实验三再按照：

> **Design → Task → Measures → Procedure → Participants → Analysis**

展开。

最后我做了全文静态检查。目前：

* `FrozenUncertain`：0处；
* `\texttt{Lost}`：0处；
* “组件归因”：0处；
* “组件消融”：0处；
* `capture-time / arrival-time`：0处；
* 英文指标只在第一次中文定义后的括号中保留；
* LaTeX花括号、主要环境均闭合；
* 无重复`\label`。

当前上传内容没有外部的`tables/`、`figures/`和`.bib`文件，所以这一轮没有声称完整`latexmk`编译通过；修改本身已经做了TeX结构检查。

[下载这一版完整稿件](sandbox:/mnt/data/egoanchor_cn_final_v2%286%29_ch5_polished.tex)

我认为现在第5章的主要框架已经比前几版稳定很多。下一轮更适合做**逐段定稿审读**，重点看实验一指标是否还要再压半段、用户研究量表介绍是否有某个构念说得过强，以及第5章和第6章是否完全一一对应，而不需要再重构章节。

我认为这版的第5章已经比前两版更接近我们想要的最终形态：**实验一负责完整系统表征，实验二简洁归因，实验三成为真正的评价重点；同时列表只留在最适合列表的场景和七项核心定制测量处。**
