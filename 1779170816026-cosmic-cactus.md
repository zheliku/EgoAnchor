# EgoAnchor 迁移重构计划 v3

## 0. 计划定位

本计划面向下一轮从 0 开始实现的 v3 重构。这里的“从 0”不是删除旧链路，而是在新目录中重新搭建架构、接口和运行时，旧 v1/v2 只作为可运行回退、算法参考和 smoke 参考。

v3 的核心目标不是简单把 v2 代码换名，而是把 EgoAnchor 明确重构为：

```text
Quest sensing stream
  -> Python external perception runtime
  -> unreliable camera-space pose observation stream
  -> Unity frame-aligned world anchor runtime
  -> reliability-aware dynamic real-object anchor
```

v3 必须同时满足三条约束：

| 约束 | v3 结论 |
|---|---|
| 视频流通信方案已确定 | Quest stereo/camera_info 只走 ZMQ 数据面，不迁移到 NATS |
| 消息通信方案已确定 | pose/status/heartbeat/command 走 NATS 消息面 |
| 旧链路仍需可用 | v1 稳定链路不动，v2 smoke/demo 不作为新主线直接继续堆代码 |

推荐新代码位置：

| 侧 | v3 位置 | 说明 |
|---|---|---|
| Python | `EgoAnchor_Python/src_v3/egoanchor/` | 从零搭建新包，复用 v2 思想和算法 wrapper 的接口设计 |
| Unity | `EgoAnchor_Unity/Assets/Scripts_v3/EgoAnchor/` | 从零搭建新脚本目录，避免 Scripts_v2 骨架继续承载历史试验 |
| 协议 | `EgoAnchor_Protocol/` | 继续作为唯一跨语言协议源；是否升级 subject/proto 版本按是否破坏兼容决定 |
| 场景 | `EgoAnchor_Unity/Assets/Scene_v3/` | v3 并行测试场景，不替换旧主场景 |

如果后续想降低初始工作量，也可以先在 `src_v2` / `Scripts_v2` 内重构。但从用户当前“重新从 0 开始实现”的需求看，更推荐 v3 新目录，能避免 v2 smoke/demo 的历史折中继续污染主线。

## 1. 对已有三份方案的适用性判断

### 1.1 `plans/plan.md`

应保留的设计：

| 设计 | v3 处理 |
|---|---|
| ZMQ 数据面 + NATS 控制/消息面 | 完整保留，作为 v3 基础架构 |
| Protobuf + `subjects.v1.json` 作为契约 | 保留，并在 v3 中强化为启动校验和代码生成来源 |
| Python 分层：transport/protocol/perception/algorithms/reliability/runtime/app/diagnostics/config | 保留，但进一步补充 routing/handlers/policy 层 |
| Python `TrackingRuntime` 是唯一 pipeline/GPU 状态 owner | 保留，作为强约束写入 v3 runtime 规则 |
| Unity frame_id 回查 capture-time camera pose | 保留，作为 v3 Anchor Runtime 的核心模块 |
| Python 输出 OpenCV camera pose，Unity 转 world anchor | 保留，避免 Python 依赖 Unity world 坐标 |
| latest-only 数据策略 | 保留，ZMQ stereo、NATS pose 都只处理最新值 |
| v2 不替换旧入口 | 保留，v3 也不替换 v1/v2，直到阶段验收完成 |

需要调整的设计：

| 设计 | 调整原因 | v3 调整 |
|---|---|---|
| `transport/nats/anchor_command_service.py` 直接注册三个 handler | 能跑通，但会随着命令增多形成局部 if/handler 膨胀 | 引入 `routing/HandlerRegistry`、`ProtobufRegistry`、`NatsRouter`，命令 handler 用装饰器注册 |
| `AnchorCommandService` 用 `set[str]` 去重 | 无 TTL、无法返回原始 ack、无法诊断 pending/executed | 使用 `CommandDedupStore`，记录 `request_id -> CommandAck`，TTL 清理 |
| `TrackingRuntime.tick()` 同时承担 poll、命令、pipeline、发布、日志 | 当前可用但 v3 应减少单体复杂度 | 拆成 `RuntimeLoop`、`CommandExecutor`、`PoseResultFactory`、`StatusPublisher`、`HeartbeatPublisher` |
| v2 reliability 只作为 pose score | 论文目标需要 anchor 侧 reliability-aware controller | Python 负责 perception quality，Unity 负责 anchor reliability gate/state/predictor，两侧明确边界 |
| Unity `NatsControlClient` 只订阅 PoseResult | v3 需要 command request、status、heartbeat | 拆成通用 NATS client + typed receivers/request client |

应废弃的设计：

| 设计 | 原因 |
|---|---|
| 把所有 v3 新能力继续塞进现有 v2 demo 入口 | 会让 smoke/demo 与正式 runtime 边界模糊 |
| 让 transport 层导入 perception 或 anchor 业务对象 | 破坏层边界，后续扩展状态/命令会变复杂 |
| 只靠字符串 phase 驱动状态 | 不足以支撑 reset/reacquire/lost/paused/recoverable failure 的工程诊断 |

### 1.2 `plans/1778748376560-curious-panda.md`

应吸收的优点：

| 设计 | v3 处理 |
|---|---|
| 注册表/装饰器避免 if-else 膨胀 | 保留，用于 NATS subject handler、command handler、protobuf type registry |
| `command_id/request_id` 幂等、TTL 去重 | 保留，作为 NATS command 必备机制 |
| command ack 与执行完成状态分离 | 保留，ack 只表示 accepted/rejected/duplicate，完成靠 status event/pose |
| NATS handler 短小，只 parse/validate/enqueue/ack | 保留，写入 v3 不变量 |
| Unity API 层封装 reset/reacquire/control | 保留，UI/交互不直接拼 subject/payload |
| 状态事件、health/heartbeat、错误分层 | 保留，纳入 v3 可观测性 |

必须调整的点：

| 原方案设计 | v3 调整 |
|---|---|
| 长期目标“全量迁移到 NATS + Protobuf，包括高频 stereo” | 与已确定技术选择冲突。v3 明确 stereo/camera_info 只走 ZMQ，NATS 不承载图像 |
| 建议统一 command subject + oneof | 当前协议已拆成 reset/reacquire/control 三个 request/reply subject。v3 可以保留三个 subject，并在应用层用统一 CommandRegistry/CommandExecutor 收敛，不强行改协议 |
| Python 新栈放 `src/anchor_server` | 当前仓库已有 `src_v2/egoanchor`，v3 应使用 `src_v3/egoanchor`，避免污染 v1 |
| Unity 新栈放 `Assets/Scripts/Comm` | 当前项目约定新工作进 `.kilo` 和 Scripts_v2/v3 风格，v3 应放 `Assets/Scripts_v3/EgoAnchor` |

应废弃的点：

| 设计 | 原因 |
|---|---|
| NATS 图像流性能 A/B 作为主迁移路径 | 技术选择已锁定 ZMQ 视频专用，v3 不再投入此方向 |
| 把 `QuestStereoFrame` 通过 NATS subject 订阅处理 | 与双平面架构冲突 |

### 1.3 `plans/1778757363496-happy-wolf.md`

应吸收的优点：

| 设计 | v3 处理 |
|---|---|
| 5 层设计：协议、传输、路由、应用、领域 | 保留，并扩展为 v3 的 8 层分层 |
| `SubjectRegistry` + `ProtobufRegistry` + `HandlerRegistry` | 保留，直接吸收 `nats_example` 的结构思想 |
| Python 异步模型分阶段：先单 loop，必要时 runtime 独立线程 | 保留，但 v3 一开始直接采用更鲁棒的“双执行域”模型 |
| `LatestInputStore`、`CommandQueue`、`CommandDedupStore` | 保留，作为 v3 runtime 边界对象 |
| 状态机状态：WAITING_INPUT、WAITING_CALIBRATION、DETECTING、REGISTERING、TRACKING、LOST、REACQUIRING、PAUSED、ERROR | 保留并细化到 Python server state 与 Unity anchor state 两套状态机 |
| 错误分层：transport/protocol/domain/runtime/recoverable tracking failure | 保留，作为日志和 status event 规范 |

必须调整的点：

| 原方案设计 | v3 调整 |
|---|---|
| NATS 可行承载高频图像流 | v3 不采用，ZMQ 固定承载 stereo/camera_info |
| `NatsQuestInput` 适配 pipeline | 改为 `ZmqQuestInput` / `QuestStreamReceiver`，但保留 latest store 接口思想 |
| Unity `QuestStreamPublisher` 通过 NATS 发布 | 改为 ZMQ publisher，NATS 仅收发低频消息和命令 |
| 以 v2 overlay 方式快速替代键盘 r | v3 从新 runtime 搭建，但 reset/reacquire 语义仍复用 |

应废弃的点：

| 设计 | 原因 |
|---|---|
| 对 NATS stereo 的吞吐风险缓解路径 | v3 不使用该路径 |
| 让 NATS router 订阅 Unity->Python stereo/camera_info | v3 的 NATS router 只挂 command subjects |

### 1.4 `nats_example/` 的取舍

`nats_example/` 不是最终代码，但它的抽象边界非常适合 v3。

建议保留为 v3 正式化设计：

| 文件 | 可吸收点 |
|---|---|
| `routing/handler_registry.py` | 装饰器注册、handler 只做轻逻辑、同步/异步兼容 |
| `routing/protobuf_registry.py` | full protobuf name -> generated class 的统一 parse/serialize |
| `transport/nats_router.py` | subject registry + protobuf registry + handler registry 的路由组合 |
| `runtime/command_dedup.py` | TTL request_id 去重，重复返回 ack 副本 |
| `runtime/commands.py` | priority command queue、reset/reacquire 高于普通 control |
| `runtime/last_input_store.py` | latest-only、frame_id 去重、camera_info version 的语义 |

需要调整：

| 当前实现 | v3 调整 |
|---|---|
| `NatsRouter.attach()` 挂所有 Unity->Python subject | 只挂 NATS transport 的 command subjects；ZMQ subject 不由 NATS router 处理 |
| protobuf import 路径指向 v2 | v3 应通过 `egoanchor.protocol` 包级入口或 v3 generated import shim |
| `LatestInputStore` 命名偏 NATS | 改为 transport-agnostic `LatestQuestInputStore`，由 ZMQ receiver 写入 |

## 2. v3 总体架构

### 2.1 双平面通信架构

v3 固定采用双平面：

| 平面 | 传输 | 方向 | 数据 | 策略 |
|---|---|---|---|---|
| Data Plane | ZMQ PUB/SUB | Unity -> Python | `QuestStereoFrame`、`QuestCameraInfo` | multipart `[topic_utf8, protobuf_bytes]`，topic latest-drain |
| Message Plane | NATS Core pub/sub | Python -> Unity | `PoseResult`、`AnchorStatusEvent`、`ServerHeartbeat` | latest-only 或 event stream，低频小 payload |
| Command Plane | NATS Core request/reply | Unity -> Python | `ResetTrackingRequest`、`ReacquireAnchorRequest`、`AnchorControlRequest` | request_id 幂等，快速 ack，runtime 串行执行 |

v3 不使用：

| 不使用 | 原因 |
|---|---|
| NATS 图像流 | 视频流方案已确定为 ZMQ 专用，避免 broker/GC/队列压力 |
| JetStream 实时链路 | 实时 pose/视频是 latest-only，不需要持久化 |
| WebRTC 图像传输 | 已明确废弃，不恢复 |
| ZMQ command request/reply | 命令、状态、ack 统一由 NATS 处理 |

### 2.2 数据流

```text
Unity Quest Sensor Layer
  StereoFrameSource
    -> FramePoseHistory.write(frame_id, left_camera_world_pose, sender_mono_ms, unity_frame)
    -> ZmqTopicPublisher.send(egoanchor.v1.quest.stereo, QuestStereoFrame)
  CameraInfoSource
    -> ZmqTopicPublisher.send(egoanchor.v1.quest.camera_info, QuestCameraInfo)

Python Data Plane
  ZmqTopicSubscriber
    -> QuestStreamReceiver
    -> LatestQuestInputStore

Python Runtime
  TrackingRuntime
    -> QuestPosePipeline
    -> PoseObservation(camera-space, diagnostics)
    -> PoseQualityScorer
    -> PoseResultFactory
    -> NatsPublisher.publish(egoanchor.v1.pose.result)
    -> Status/Heartbeat publishers

Unity Message Plane
  NatsControlClient
    -> PoseResultReceiver latest-drain
    -> PoseToAnchorRuntime
    -> CameraPoseFrameAligner(frame_id -> capture-time camera pose)
    -> AnchorPolicyController(reliability gate/state/predict/filter)
    -> DynamicObjectAnchor Transform

Unity Command API
  AnchorCommandClient.Reset/Reacquire/Control
    -> NATS request/reply
    -> Python NatsRouter
    -> HandlerRegistry
    -> CommandDedupStore
    -> CommandQueue
    -> TrackingRuntime command executor
    -> AnchorStatusEvent / PoseResult reflects outcome
```

### 2.3 依赖方向

v3 必须强制单向依赖：

```text
app
  -> runtime
  -> application services / policy
  -> perception / anchor domain
  -> algorithms
  -> protocol
  -> transport
```

更具体地说：

| 层 | 可以依赖 | 不允许依赖 |
|---|---|---|
| `transport` | 第三方 socket/client、基础配置 | Protobuf 业务类型、perception、Unity anchor 语义 |
| `protocol` | generated protobuf、subjects registry | transport socket、pipeline、Unity Transform |
| `routing` | protocol registry、handler registry、transport client 抽象 | pipeline/GPU、OpenCV、Unity anchor |
| `handlers` | command queue、latest store、dedup、ack factory | FoundationPose、Transform、耗时操作 |
| `runtime` | receiver store、command queue、perception、publishers | Unity world transform |
| `perception` | algorithms、calibration、diagnostics | ZMQ/NATS、Unity |
| `policy/reliability` | observation/state/diagnostics | transport details |
| `app` | 组装所有对象 | 具体算法内层细节 |

## 3. 协议与契约设计

### 3.1 保留当前协议主线

当前 `EgoAnchor_Protocol/subjects.v1.json` 已经符合 v3 双平面方向：

| Subject | Transport | v3 处理 |
|---|---|---|
| `egoanchor.v1.quest.stereo` | ZMQ | 保留，data plane 高频 stereo |
| `egoanchor.v1.quest.camera_info` | ZMQ | 保留，data plane 低频标定 |
| `egoanchor.v1.pose.result` | NATS | 保留，message plane latest pose |
| `egoanchor.v1.anchor.status` | NATS | 保留，状态事件流 |
| `egoanchor.v1.server.heartbeat` | NATS | 保留，健康心跳 |
| `egoanchor.v1.cmd.anchor.reset` | NATS request/reply | 保留，清空 tracking |
| `egoanchor.v1.cmd.anchor.reacquire` | NATS request/reply | 保留，主动重新获取 |
| `egoanchor.v1.cmd.anchor.control` | NATS request/reply | 保留，pause/resume/stage 等控制 |

v3 初期可以继续使用 `protocol.v1` schema。只有在字段语义破坏兼容时才升级到 `protocol.v2` 或 `egoanchor.v2.*` subjects。

### 3.2 建议补强但不强制首阶段完成的字段

当前 `PoseResult` 已有 `phase/stage/det_count/depth_valid_ratio/timing/last_error`。v3 为论文与鲁棒性需要建议追加字段，不重排现有字段号：

| 消息 | 建议字段 | 目的 |
|---|---|---|
| `PoseResult` | `float reliability_score` | Unity gate/filter 可直接使用 Python 感知可信度 |
| `PoseResult` | `repeated string reliability_flags` | 解释 reject/hold 的原因 |
| `PoseResult` | `float depth_valid_in_mask` | 判断 mask-depth 对齐 |
| `PoseResult` | `float mask_area_ratio` | 判断分割异常 |
| `PoseResult` | `string pose_source` | `REGISTER/TRACK/RE_REGISTER/HOLD/NONE` |
| `PoseResult` | `double server_receive_mono_ms`、`server_publish_mono_ms` | 端到端诊断 |
| `AnchorStatusEvent` | `int64 generation` | reset/reacquire 后防止旧结果污染 |
| `ServerHeartbeat` | `bool camera_info_ready`、`int32 dropped_stale_frames` | 输入健康诊断 |
| `CommandAck` | `string state`、`int32 queue_length` | Unity UI 解释命令接受状态 |

若短期不改 proto，v3 仍需在内部数据结构中预留这些字段，避免后续大改接口。

### 3.3 协议生成策略

v3 使用 `EgoAnchor_Protocol/tools/generate_proto.ps1` 作为默认协议生成入口。默认输出到 v3；如需兼容刷新旧 v2，可显式使用 `-GenerateV2`。

建议目标：

| 输出 | 路径 |
|---|---|
| Python v3 generated | `EgoAnchor_Python/src_v3/egoanchor/protocol/v1/*_pb2.py` |
| Unity v3 generated | `EgoAnchor_Unity/Assets/Scripts_v3/EgoAnchor/Protocol/Generated/*.cs` |
| Unity v3 subject constants | `EgoAnchor_Unity/Assets/Scripts_v3/EgoAnchor/Protocol/SubjectNames.cs` |

协议层必须增加启动校验：

| 校验 | 失败处理 |
|---|---|
| `subjects.v1.json` 中每个 protobuf full name 都能在 generated registry 找到 | server 启动失败 |
| 每个 NATS request/reply subject 都定义 reply type | server 启动失败 |
| ZMQ subject 不被 NATS router attach | 单测失败 |
| Unity 业务代码只使用 generated `SubjectNames`，不手写 subject 字符串 | 编译/单测失败 |

## 4. Python v3 架构

### 4.1 目录结构

```text
EgoAnchor_Python/src_v3/egoanchor/
  __init__.py

  app/
    __init__.py
    tracking_server.py
    smoke_server.py
    probes.py

  config/
    __init__.py
    defaults.toml
    runtime_config.py
    schema.py

  protocol/
    __init__.py
    subjects.py
    protobuf_registry.py
    v1/
      common_pb2.py
      quest_pb2.py
      anchor_pb2.py

  transport/
    __init__.py
    zmq_topic_subscriber.py
    nats_client.py

  routing/
    __init__.py
    handler_registry.py
    nats_router.py
    route_specs.py

  handlers/
    __init__.py
    command_handlers.py
    status_handlers.py

  runtime/
    __init__.py
    latest_quest_input_store.py
    quest_stream_receiver.py
    command_queue.py
    command_dedup.py
    command_models.py
    command_executor.py
    tracking_runtime.py
    runtime_loop.py
    runtime_state.py
    pose_result_factory.py
    status_event_factory.py
    heartbeat_factory.py

  perception/
    __init__.py
    quest_frame.py
    quest_calibration.py
    pose_observation.py
    quest_pose_pipeline.py
    pipeline_factory.py

  algorithms/
    __init__.py
    yoloe26_segmenter.py
    sam3_segmenter.py
    fast_foundationstereo_depth.py
    foundationpose_estimator.py
    cutie_mask_tracker.py

  reliability/
    __init__.py
    pose_quality.py
    pose_gate.py
    failure_reason.py

  diagnostics/
    __init__.py
    runtime_stats.py
    debug_view.py
    event_log.py
    window.py

  tests/
    __init__.py
    test_subject_contract.py
    test_protobuf_registry.py
    test_handler_registry.py
    test_zmq_latest_store.py
    test_command_dedup.py
    test_command_queue.py
    test_runtime_with_fake_pipeline.py
    test_pose_result_factory.py
```

### 4.2 Python 分层职责

| 层 | 主要职责 | 输入 | 输出 |
|---|---|---|---|
| `transport` | ZMQ/NATS 连接、send/recv bytes、重连、关闭、统计 | endpoint/subject/bytes | bytes 或低层事件 |
| `protocol` | subjects registry、protobuf type registry、parse/serialize | subject + bytes | typed protobuf |
| `routing` | NATS subject -> handler 分发 | NATS message bytes | reply bytes 或 None |
| `handlers` | parse 后轻量校验、dedup、入队、ack | typed command protobuf | `CommandAck` |
| `runtime` | 单一 pipeline owner、命令执行、输入快照、发布结果 | latest input + commands | pose/status/heartbeat |
| `perception` | Quest frame/calibration -> camera-space pose observation | `QuestStereoFrame` + `QuestCameraInfo` | `PoseObservation` |
| `algorithms` | 单模型 wrapper | image/mask/depth | model-specific outputs |
| `reliability` | 感知质量评分、失败原因、pose gate | `PoseObservation` | score/flags/reason |
| `diagnostics` | debug view、日志、事件记录、统计 | runtime events | visual/log/metrics |
| `app` | 加载配置、组装依赖、启动/停止生命周期 | CLI/config | running service |

### 4.3 Python 异步与并发模型

v3 推荐一开始采用“双执行域”模型，而不是把 NATS I/O 与 GPU pipeline 放在同一个 asyncio loop 中。

| 执行域 | 技术 | 负责 | 不能做 |
|---|---|---|---|
| Control I/O domain | 后台 asyncio loop/thread | NATS connect、subscribe、request handler、publish future | 运行 pipeline、OpenCV 显示、修改 FoundationPose |
| Runtime domain | 主线程或专用 runtime 线程 | ZMQ poll、latest store、pipeline、command execution、debug window | 阻塞 NATS callback、直接处理 request reply |

初始实现建议：

| 组件 | 线程安全策略 |
|---|---|
| `LatestQuestInputStore` | 只由 runtime 写入，因为 ZMQ poll 在 runtime domain 中执行 |
| `CommandQueue` | NATS handler 写，runtime 读；使用 `queue.Queue` 或 lock 保护 priority queue |
| `CommandDedupStore` | NATS handler 独占即可；如 runtime 需要更新执行完成状态，则用 lock |
| `NatsPublisher` | runtime 调用非阻塞 `publish_nowait`，内部投递到 NATS loop，限制 pending futures |
| `TrackingRuntime` | 单线程 owner，任何 command 都在 frame 边界执行 |

NATS handler 不变量：

```text
parse protobuf
  -> validate request_id/anchor_id/basic params
  -> dedup check
  -> enqueue RuntimeCommand
  -> return CommandAck bytes
```

NATS handler 永远不做：

| 禁止事项 | 原因 |
|---|---|
| 调 `pipeline.run/process` | GPU/模型状态并发风险 |
| 调 `pipeline.reset_tracking_state` | 可能打断当前帧处理 |
| 打开 OpenCV 窗口 | 非 I/O 线程职责 |
| 等待 reacquire 成功 | request/reply 会阻塞，且 ack 语义会混淆 |
| 修改 Unity world pose 相关字段 | Python 不拥有 Unity world 坐标 |

### 4.4 注册表与装饰器机制

v3 正式引入三类 registry。

`SubjectRegistry`：

| 职责 | 说明 |
|---|---|
| 从 `subjects.v1.json` 加载 subject spec | 包含 transport、direction、protobuf、reply、mode、latest_only |
| 按 transport 过滤 | ZMQ spec 给 data plane，NATS spec 给 router |
| 启动校验 | protobuf type 存在、reply type 存在、mode 合法 |

`ProtobufRegistry`：

| 职责 | 说明 |
|---|---|
| 扫描 generated pb2 module | full name -> class |
| `parse(subject, payload)` | 通过 subject spec 找 protobuf class |
| `serialize(message)` | 统一序列化 |

`HandlerRegistry`：

| 职责 | 说明 |
|---|---|
| `@registry.request(subject)` | 注册 request/reply handler |
| `@registry.subscribe(subject)` | 注册 NATS pub/sub handler，v3 首期主要用于未来 Python 接收 debug/config，不处理 ZMQ |
| 检查重复注册 | 启动时报错 |
| 检查缺 handler | NATS request subject 无 handler 则启动失败 |

示例形态：

```python
registry = HandlerRegistry()

@registry.request(CMD_ANCHOR_RESET)
def handle_reset(ctx: HandlerContext, msg: ResetTrackingRequest) -> CommandAck:
    return ctx.command_acceptor.accept_reset(msg)

@registry.request(CMD_ANCHOR_REACQUIRE)
def handle_reacquire(ctx: HandlerContext, msg: ReacquireAnchorRequest) -> CommandAck:
    return ctx.command_acceptor.accept_reacquire(msg)
```

### 4.5 Runtime 状态机

Python v3 server state 负责描述 perception/runtime 状态，不负责 Unity anchor display state。

推荐状态：

| 状态 | 含义 | 进入条件 | 退出条件 |
|---|---|---|---|
| `BOOTING` | 配置/模型/网络初始化中 | app start | 初始化完成或失败 |
| `WAITING_INPUT` | 尚未收到 stereo | ZMQ started 但无 stereo | 收到 stereo |
| `WAITING_CALIBRATION` | stereo 已有但缺 camera_info | 有图像无标定 | 收到有效 camera_info |
| `DETECTING` | 等待有效 mask/depth/register | reset 后或无 pose | register 成功 |
| `REGISTERING` | 当前帧正在 register | 有 mask/depth，执行 register | 成功到 TRACKING，失败回 DETECTING |
| `TRACKING` | 持续 track 或 re-register | pose 有效 | track lost/jump/reset/pause |
| `LOST` | 可恢复跟踪丢失 | 连续 reject/无有效 pose | re-register 成功或 reset |
| `REACQUIRING` | 用户主动请求重新获取 | reacquire command | 成功/超时/取消 |
| `PAUSED` | runtime 暂停处理 pipeline | pause command | resume/reset |
| `ERROR` | 不可恢复 runtime 错误 | pipeline/model/network fatal | 手动 reset/restart |
| `STOPPED` | 正常退出 | stop | 无 |

状态输出规则：

| 输出 | 频率 | 内容 |
|---|---|---|
| `PoseResult` | 每个新 processed frame，latest-only | has_pose、pose、phase、diagnostics |
| `AnchorStatusEvent` | 状态变化、命令执行结果、重要失败 | state、event、message、error、generation |
| `ServerHeartbeat` | 固定低频，例如 1Hz | state、input_ready、latest_frame、queue_length、last_error |

### 4.6 Command 语义

`ResetTrackingRequest`：

| 项 | v3 语义 |
|---|---|
| ack | request_id 有效且命令入队即 `accepted=true` |
| runtime 执行 | 在 frame 边界清空 FoundationPose/Cutie/last pose，generation++ |
| status | 发布 `RESET_APPLIED` |
| pose | 可发布一条 `PoseResult(has_pose=false, phase="RESET")`，帮助 Unity 状态同步 |
| Unity 本地 | 收到 ack 或 status 后清空 local processors，按配置决定是否清空 anchor pose |

`ReacquireAnchorRequest`：

| 项 | v3 语义 |
|---|---|
| ack | 只表示进入 reacquire 流程 |
| `NEXT_VALID_FRAME` | reset 后等待下一帧有效 mask/depth register |
| `LATEST_FRAME_IF_AVAILABLE` | 若已有 latest stereo/calib，则尽快处理最新帧，否则等待 |
| `FORCE_DETECT` | 强制清空 tracking，并短期禁用 track，优先 register |
| status | `REACQUIRE_STARTED`、`REACQUIRE_SUCCEEDED`、`REACQUIRE_FAILED/TIMEOUT` |
| 超时 | 不阻塞 request/reply，通过 status event 通知 |

`AnchorControlRequest`：

| Action | v3 首期支持 | 说明 |
|---|---|---|
| `SET_STAGE` | 支持 | stage 1..4，切换时 reset tracking |
| `PAUSE` | 支持 | runtime 不运行 pipeline，但仍可 poll input 和 heartbeat |
| `RESUME` | 支持 | 恢复 runtime |
| 未来 `SET_PROMPT` | 后续 | 涉及 YOLOE prompt 和 tracker reset |
| 未来 `SET_CONFIG` | 后续 | 高风险，需 schema 和权限策略 |

Command 队列策略：

| 规则 | v3 处理 |
|---|---|
| reset/reacquire 优先于普通 control | priority queue |
| 队列满 | handler 返回 `accepted=false, status=QUEUE_FULL` |
| 重复 request_id | 返回缓存 ack，`duplicate=true`，不重复入队 |
| 不同 request_id 内容相同 | 视为新命令 |
| pause/resume 幂等 | 已 paused 再 pause 返回 accepted no-op status |

### 4.7 Python 感知 pipeline

v3 的 `QuestPosePipeline` 应保留 v2 已验证的算法顺序，但接口更干净：

```text
QuestStereoFrame + QuestCameraInfo
  -> decode JPEG
  -> K remapping / preprocessing
  -> YOLOE-26 segmenter
  -> Fast-FoundationStereo depth
  -> FoundationPose register / track / re-register
  -> optional Cutie mask propagation
  -> PoseObservation(camera-space pose + diagnostics)
```

可从现有代码迁移的内容：

| 来源 | v3 处理 |
|---|---|
| `src_v2/egoanchor/perception/quest_pose_pipeline.py` | 复用处理流程，重写为更小函数和更稳定输出模型 |
| `src_v2/egoanchor/perception/quest_calibration.py` | 复用 K remapping 和 camera_info proto 转换 |
| `src_v2/egoanchor/algorithms/*` | 可直接迁移 wrapper 思路，但保持懒加载重模型 |
| `src_v2/egoanchor/reliability/pose_quality.py` | 作为 v3 pose quality 初版 |
| v1 `quest_object_tracking_pipeline.py` | 对照行为和历史坑，不直接搬单体结构 |

v3 `PoseObservation` 建议字段：

| 字段 | 含义 |
|---|---|
| `frame_id` | Unity frame alignment key |
| `has_pose` | 是否有可用 pose |
| `pose_matrix_cv_camera` | OpenCV camera 坐标 4x4 row-major |
| `pose_source` | `REGISTER/TRACK/RE_REGISTER/NONE` |
| `phase` | 调试阶段或失败 phase |
| `tracking_state_hint` | perception 侧状态提示 |
| `depth_valid_ratio` | 全图有效深度比例 |
| `depth_valid_in_mask` | mask 内有效深度比例 |
| `mask_area_ratio` | mask 面积比例 |
| `track_reject_count` | 连续 track reject 计数 |
| `timing` | yolo/depth/cutie/pose/total ms |
| `reliability_score` | 0..1 感知质量分 |
| `reliability_flags` | 诊断 flags |
| `failure_reason` | 结构化失败原因 |

## 5. Unity v3 架构

### 5.1 目录结构

```text
EgoAnchor_Unity/Assets/Scripts_v3/EgoAnchor/
  Protocol/
    Generated/
      Common.cs
      Quest.cs
      Anchor.cs
    SubjectNames.cs
    ProtoRegistry.cs

  Transport/
    ZmqTopicPublisher.cs
    NatsConnection.cs
    NatsPublisher.cs
    NatsSubscriber.cs
    NatsRequestClient.cs
    MainThreadDispatcher.cs

  Quest/
    StereoFrameSource.cs
    CameraInfoSource.cs
    FramePoseHistory.cs
    QuestCaptureClock.cs

  Client/
    EgoAnchorClient.cs
    QuestStreamPublisher.cs
    PoseResultReceiver.cs
    AnchorStatusReceiver.cs
    ServerHeartbeatReceiver.cs
    AnchorCommandClient.cs

  Anchor/
    PoseToAnchorRuntime.cs
    CameraPoseFrameAligner.cs
    DynamicObjectAnchor.cs
    AnchorObservation.cs
    AnchorPoseProcessor.cs
    AnchorKalmanPoseProcessor.cs
    AnchorLowPassPoseProcessor.cs
    AnchorStateMachine.cs
    AnchorLifecycleEvent.cs

  Reliability/
    ReliabilityScore.cs
    ReliabilityGate.cs
    AnchorPolicyController.cs
    AnchorPredictor.cs
    PoseInnovationGate.cs

  Diagnostics/
    EgoAnchorHud.cs
    LinkStats.cs
    AnchorDebugGizmos.cs
    EventLogPanel.cs

  Config/
    EgoAnchorSettings.cs
    NetworkSettingsStore.cs
```

### 5.2 Unity 分层职责

| 层 | 职责 | 不允许做 |
|---|---|---|
| `Transport` | ZMQ/NATS 连接、bytes send/receive、thread-safe queue | Protobuf 业务解释、Transform 修改 |
| `Protocol` | generated types、subject aliases、parse helpers | socket、MonoBehaviour 生命周期 |
| `Quest` | Passthrough texture/intrinsics/camera pose capture | 网络发送 |
| `Client` | 组合 Quest source、transport、receiver、command API | 直接实现滤波算法 |
| `Anchor` | frame alignment、raw/stable pose、anchor state | 网络收发 |
| `Reliability` | gate/filter/predict/state policy | Protobuf 解码、NATS 请求 |
| `Diagnostics` | HUD、stats、gizmos、event log | 改变 tracking 状态 |
| `Config` | endpoint、NATS URL、fps、HWM、持久化设置 | 硬编码实验参数 |

### 5.3 Unity 关键组件

`Quest/StereoFrameSource.cs`：

| 职责 | 说明 |
|---|---|
| 读取左右 Passthrough texture | 保持 Quest API 细节封装 |
| JPEG 编码 | 输出 `QuestStereoFrame` protobuf |
| 分配 `frame_id` | 单调递增，不复用 |
| 写 `FramePoseHistory` | 记录 capture-time 左目 camera world pose |
| 写 header timing | `sender_mono_ms`、`unity_frame` |
| 不负责 ZMQ | 网络由 `QuestStreamPublisher` 调用 transport |

`Quest/FramePoseHistory.cs`：

| 职责 | 说明 |
|---|---|
| 环形缓存 `frame_id -> camera pose` | v3 frame-aligned anchor 核心 |
| 缓存 sender_mono_ms/unity_frame | 延迟和调试 |
| 支持 miss 诊断 | 过期、未写入、frame_id 倒退 |
| 不做滤波 | 只提供 capture-time camera pose |

`Client/QuestStreamPublisher.cs`：

| 职责 | 说明 |
|---|---|
| 按 stereoFps/cameraInfoFps 调度 | 类似 v2，但 v3 配置化 |
| 用 `ZmqTopicPublisher` 发送 protobuf bytes | subject 来自 generated `SubjectNames` |
| latest-only 发送策略 | HWM 小、send fail 计 drop |
| 聚合统计 | 不每帧刷日志 |

`Transport/NatsConnection.cs`：

| 职责 | 说明 |
|---|---|
| 单一 NATS 连接生命周期 | connect/reconnect/dispose |
| 低层 publish/subscribe/request | 不解析 anchor 业务 |
| PlayerPrefs/UI 注入 URL | 不写死 Quest 真机 IP |
| callback 入主线程队列 | 不直接访问 UnityEngine API |

`Client/AnchorCommandClient.cs`：

| API | 语义 |
|---|---|
| `ResetTrackingAsync(anchorId, clearFilters, clearAnchorPose, reason, ct)` | 发送 reset request，返回 `CommandAck` |
| `ReacquireAsync(anchorId, mode, timeoutMs, ct)` | 发送 reacquire request，完成靠 status/pose 判断 |
| `SetStageAsync(stage, ct)` | 发送 control request |
| `PauseAsync(ct)` / `ResumeAsync(ct)` | 发送 control request |

`Client/PoseResultReceiver.cs`：

| 职责 | 说明 |
|---|---|
| 订阅 `egoanchor.v1.pose.result` | NATS latest-only 队列 |
| 主线程 parse protobuf | callback 不触摸 Unity API |
| 丢旧保新 | 只处理最新 pose result |
| 转交 `PoseToAnchorRuntime` | 不修改 Transform |

`Anchor/CameraPoseFrameAligner.cs`：

| 职责 | 说明 |
|---|---|
| 输入 Python OpenCV camera pose | `PoseResult.pose_matrix_cv_camera` |
| 查询 `FramePoseHistory` | 按 `frame_id` 找 capture-time camera world pose |
| OpenCV -> Unity camera coordinates | y 轴转换，保持旧约定 |
| camera-local -> world pose | 输出 raw world anchor pose |
| 输出 failure reason | invalid matrix、history miss、conversion failed |

`Anchor/PoseToAnchorRuntime.cs`：

| 职责 | 说明 |
|---|---|
| 接收 `PoseResult` | 业务入口 |
| 维护 raw pose | 论文 baseline |
| 维护 stable pose | processor/policy 输出 |
| 驱动 AnchorStateMachine | has_pose=false、align miss、low reliability 等 |
| 不负责网络 | 只消费已解码 result |

`Reliability/AnchorPolicyController.cs`：

| 输入 | 输出 | 说明 |
|---|---|---|
| raw world pose | accept/reject/hold/predict | 负责 anchor 侧鲁棒性 |
| Python reliability score/flags | state transition reason | 不重新跑 perception |
| frame delta / velocity / innovation | stable pose | 支撑低频/延迟/间歇失效 |

### 5.4 Unity Anchor 状态机

Unity anchor state 与 Python server state 分离。

推荐状态：

| 状态 | 含义 |
|---|---|
| `Uninitialized` | 尚未收到有效 pose 或未配置 history/runtime |
| `Searching` | Python 正在检测/注册，Unity 无稳定 anchor |
| `Tracking` | 持续收到可靠 pose，正常更新 anchor |
| `Coasting` | 短时间无 pose 或低频延迟，用 predictor/last velocity 续航 |
| `FrozenUncertain` | pose 不可靠但不立即清空 anchor，冻结显示并标记不确定 |
| `Lost` | 长时间无可靠 pose，停止更新或隐藏/提示 |
| `Relocalizing` | 用户或系统主动 reacquire 中 |
| `Paused` | 用户暂停 anchor 更新 |
| `Error` | 协议/对齐/runtime 错误需要人工处理 |

状态转移依据：

| 输入 | 影响 |
|---|---|
| `PoseResult.has_pose=false` | Tracking -> Coasting/Frozen/Lost，取决于持续时间和 failure reason |
| frame history miss | 计入 align failure，连续 miss 进入 Frozen/Lost |
| low reliability score | reject 或降低 filter 权重 |
| large pose jump | gate reject，可能进入 FrozenUncertain |
| reset/reacquire status event | 进入 Searching/Relocalizing，清理本地 processors |
| heartbeat disconnected | 进入 FrozenUncertain 或 Lost |
| pause/resume command | Paused <-> previous active state |

## 6. v3 可落地迁移阶段

### Phase 0：冻结旧线与定义 v3 边界

目标：明确 v3 新目录和旧线回退边界，不开始搬复杂算法。

产物：

| 产物 | 路径 |
|---|---|
| Python v3 包骨架 | `EgoAnchor_Python/src_v3/egoanchor/` |
| Unity v3 脚本目录骨架 | `Assets/Scripts_v3/EgoAnchor/` |
| v3 场景工作区 | `Assets/Scene_v3/` |
| v3 默认配置草案 | `src_v3/egoanchor/config/defaults.toml` |
| 协议生成脚本 v3 输出支持 | `EgoAnchor_Protocol/tools/generate_proto.ps1` |

验收：

| 验收项 | 命令/方式 |
|---|---|
| v1 不受影响 | `pixi run python .\src\object_tracking_server.py --print_config` |
| v2 不受影响 | `pixi run python -m unittest discover -s src_v2 -p "test_*.py"` |
| Unity 仍能编译 | `dotnet build "EgoAnchor_Unity\Assembly-CSharp.csproj" --no-restore` |

风险：

| 风险 | 缓解 |
|---|---|
| v3 目录与 v2 generated 冲突 | 生成脚本参数化输出，不复用同一个 Generated 目录 |
| 开始阶段文件多但功能少 | Phase 0 只建骨架和契约测试，不接模型 |

### Phase 1：协议注册表与 NATS command smoke

目标：把 `nats_example` 的 registry/router/dedup 机制正式化，但只处理 NATS command，不碰视频流。

产物：

| Python 模块 | 职责 |
|---|---|
| `protocol/subjects.py` | 加载 `subjects.v1.json`，按 transport/mode 过滤 |
| `protocol/protobuf_registry.py` | full name -> protobuf class |
| `routing/handler_registry.py` | 装饰器注册 handler |
| `routing/nats_router.py` | NATS request/reply bytes -> typed handler -> reply bytes |
| `runtime/command_dedup.py` | request_id TTL 去重 |
| `runtime/command_queue.py` | priority queue |
| `handlers/command_handlers.py` | reset/reacquire/control acceptor |
| `app/smoke_server.py` | 不加载模型的 command smoke server |

验收：

| 验收项 | 说明 |
|---|---|
| subject registry 只给 NATS router 挂 NATS command subjects | ZMQ stereo/camera_info 不被 NATS router 订阅 |
| reset request protobuf roundtrip | Python 返回 `CommandAck(accepted=true)` |
| 重复 request_id | 返回 `duplicate=true`，队列长度不增加 |
| invalid protobuf | 返回 `accepted=false, status=DECODE_FAILED` 或空 reply + error log |
| 队列满 | 返回 `QUEUE_FULL` |

风险：

| 风险 | 缓解 |
|---|---|
| 过早抽象导致复杂 | 只抽象 subject/protobuf/handler 三个稳定点 |
| handler 异常导致 NATS 无 reply | router 统一捕获并返回结构化 failure ack |

### Phase 2：Python ZMQ data plane 与 latest input store

目标：建立正式 ZMQ 视频数据面，不接模型，只验证 Quest stream 输入、latest-only、camera_info version。

产物：

| 模块 | 职责 |
|---|---|
| `transport/zmq_topic_subscriber.py` | 通用 multipart topic subscriber，不导入 protobuf |
| `runtime/latest_quest_input_store.py` | stereo latest、camera_info latest、frame_id 去重、version |
| `runtime/quest_stream_receiver.py` | ZMQ bytes -> Quest protobuf -> latest store |
| `app/probes.py` | video/camera_info probe |

验收：

| 验收项 | 说明 |
|---|---|
| fake sender 可发送 stereo/camera_info | Python 显示 frame_id 递增 |
| topic independent cache | stereo 高频不会覆盖 camera_info |
| stale frame drop | frame_id 不递增的 stereo 被拒绝计数 |
| invalid multipart/protobuf | 记录统计，不崩溃 |
| 无 NATS 时仍可运行 ZMQ probe | 数据面与消息面解耦 |

风险：

| 风险 | 缓解 |
|---|---|
| ZMQ PUB/SUB 启动顺序丢首帧 | camera_info 低频重复发布，runtime 等待 ready |
| Quest 端发送太快 | HWM 小、latest-drain、统计 drop |

### Phase 3：Unity v3 data plane 发送

目标：Unity v3 能独立发送 Quest stereo/camera_info，并写入 frame pose history。

产物：

| Unity 模块 | 职责 |
|---|---|
| `Transport/ZmqTopicPublisher.cs` | 通用 ZMQ multipart publisher |
| `Quest/FramePoseHistory.cs` | capture-time camera pose 环形缓存 |
| `Quest/StereoFrameSource.cs` | texture/JPEG/protobuf/header/frame pose history |
| `Quest/CameraInfoSource.cs` | intrinsics/lens pose/protobuf |
| `Client/QuestStreamPublisher.cs` | 调度发送、统计、配置 |

验收：

| 验收项 | 说明 |
|---|---|
| Editor/fake mode 能发送 protobuf | Python Phase 2 probe 可收到 |
| Quest 真机可发送 stereo/camera_info | Python 显示尺寸、fps、frame_id、camera_info version |
| `FramePoseHistory` 命中写入 | 每个成功 stereo capture 写同 frame_id pose |
| 主线程不被网络阻塞 | send fail 计 drop，不等待 |

风险：

| 风险 | 缓解 |
|---|---|
| Quest texture/JPEG 分配导致 GC | 初期功能正确，Phase 8 profile 后再 buffer reuse |
| frame pose 写入和发送不一致 | `StereoFrameSource.TryCapture()` 内同一事务生成 frame 和写 history |

### Phase 4：Python perception runtime 接入 fake/real pipeline

目标：建立 `TrackingRuntime` 正式主循环，先 fake pipeline，再接真实 YOLOE/FFS/FoundationPose。

产物：

| 模块 | 职责 |
|---|---|
| `perception/pose_observation.py` | v3 observation 数据结构 |
| `perception/quest_pose_pipeline.py` | 真实 pipeline |
| `perception/pipeline_factory.py` | fake/real pipeline 构建 |
| `runtime/tracking_runtime.py` | 单一 pipeline owner |
| `runtime/command_executor.py` | runtime 内执行 reset/reacquire/control |
| `runtime/pose_result_factory.py` | observation -> `PoseResult` |
| `runtime/status_event_factory.py` | state/command result -> `AnchorStatusEvent` |
| `runtime/heartbeat_factory.py` | runtime stats -> `ServerHeartbeat` |

验收：

| 验收项 | 说明 |
|---|---|
| fake pipeline 端到端输出 PoseResult | 无 GPU 也可跑测试 |
| reset command 在 runtime 内执行 | handler 不直接碰 pipeline |
| pause/resume/set_stage 可控 | 状态和 heartbeat 正确 |
| 真实 pipeline 可处理 Quest 输入 | 输出 camera-space pose |
| duplicate command 不重复 reset | generation 不重复增加 |

风险：

| 风险 | 缓解 |
|---|---|
| 模型加载慢影响 smoke | fake pipeline 是默认测试路径，real pipeline 通过配置启用 |
| command 与 frame 处理中途冲突 | `_drain_commands()` 只在 frame 边界执行 |
| NATS publish 断连积压 | publish_nowait pending 上限，超限丢旧 pose |

### Phase 5：Unity v3 message plane 与 command API

目标：Unity 能通过 NATS 接 pose/status/heartbeat，并能发送 command request。

产物：

| Unity 模块 | 职责 |
|---|---|
| `Transport/NatsConnection.cs` | NATS 生命周期 |
| `Transport/NatsSubscriber.cs` | typed/latest/event subscribe helper |
| `Transport/NatsRequestClient.cs` | request/reply、timeout、request_id |
| `Transport/MainThreadDispatcher.cs` | 后台回调切主线程 |
| `Client/PoseResultReceiver.cs` | latest PoseResult -> Anchor runtime |
| `Client/AnchorStatusReceiver.cs` | status event -> UI/state bridge |
| `Client/ServerHeartbeatReceiver.cs` | heartbeat -> link health |
| `Client/AnchorCommandClient.cs` | reset/reacquire/control API |
| `Client/EgoAnchorClient.cs` | 场景级组合组件 |

验收：

| 验收项 | 说明 |
|---|---|
| Unity reset ContextMenu 收到 ack | Python smoke server 返回 protobuf ack |
| Timeout 与 rejected 区分 | API 结果不混淆 transport/domain failure |
| PoseResult latest-only | 短时多条只处理最新 |
| Status event 不 latest-only | 可记录最近 N 条事件 |
| NATS callback 不访问 Transform | 只入队，Update drain |

风险：

| 风险 | 缓解 |
|---|---|
| NATS.Net Quest IL2CPP 风险 | Editor 先完成，Quest 真机单独验收；必要时保留 Unity 只接 pose 的最小子集 |
| request_id 生成混乱 | `AnchorCommandClient` 统一生成，重试复用同一 request_id |

### Phase 6：Unity pose-to-anchor runtime

目标：完成 frame-aligned raw world anchor、processor chain、基础状态机。

产物：

| Unity 模块 | 职责 |
|---|---|
| `Anchor/CameraPoseFrameAligner.cs` | OpenCV camera pose + frame history -> Unity world pose |
| `Anchor/PoseToAnchorRuntime.cs` | raw/stable pose 管理 |
| `Anchor/AnchorPoseProcessor.cs` | processor 基类 |
| `Anchor/AnchorKalmanPoseProcessor.cs` | 常速度 Kalman baseline |
| `Anchor/AnchorLowPassPoseProcessor.cs` | 指数平滑 baseline |
| `Anchor/DynamicObjectAnchor.cs` | Transform apply |
| `Anchor/AnchorStateMachine.cs` | 初版 anchor lifecycle |

验收：

| 验收项 | 说明 |
|---|---|
| fake PoseResult + fake frame history 可对齐 | 不依赖 Python 模型 |
| `has_pose=false` 不应用 transform | 但状态收到更新 |
| frame history miss 可诊断 | `align_failed_frame_x` 统计 |
| raw 与 stable 可切换 | 支撑论文 baseline |
| reset ack/status 清空 processors | local smoother 不保留旧状态 |

风险：

| 风险 | 缓解 |
|---|---|
| 坐标转换错误 | 单测覆盖 OpenCV->Unity camera、camera-local->world；对照 v1 `PoseDecoder/FrameAlignedObjectAnchor` |
| 使用到达时 HMD pose | 禁止。只能使用 `FramePoseHistory` 中 capture-time pose |

### Phase 7：Reliability-aware anchor controller

目标：把论文核心从“能显示 pose”提升为“低频、延迟、噪声、间歇失效下的稳定 dynamic anchor”。

产物：

| Unity 模块 | 职责 |
|---|---|
| `Reliability/ReliabilityGate.cs` | 根据 score/flags/innovation 过滤 pose |
| `Reliability/PoseInnovationGate.cs` | 平移/旋转跳变检测 |
| `Reliability/AnchorPredictor.cs` | 短时 coasting 预测 |
| `Reliability/AnchorPolicyController.cs` | gate/filter/predict/state 组合 |
| `Diagnostics/EventLogPanel.cs` | 状态/拒绝原因可视化 |

策略初版：

| 情况 | 行为 |
|---|---|
| score 高、frame_id 命中、jump 小 | accept，更新 raw/stable |
| score 低但短时 | reject pose，保持 stable，进入 `FrozenUncertain` |
| pose 暂缺但时间短 | predictor/coast，进入 `Coasting` |
| 连续缺失/低可信超阈值 | 进入 `Lost` |
| reset/reacquire | 清理 predictor/filter，进入 `Searching/Relocalizing` |

验收：

| 验收项 | 说明 |
|---|---|
| 人工注入 pose jump 被拒绝 | stable 不瞬移 |
| 人工丢 pose 可短时 coasting | 不立即消失或乱跳 |
| 长时间无 pose 进入 Lost | 状态可视化 |
| raw/simple smooth/reliability-aware 三种输出可对比 | 支撑实验 |

风险：

| 风险 | 缓解 |
|---|---|
| 过度滤波导致滞后 | 保留 raw 和 simple baseline，参数配置化 |
| controller 被写成“已完成贡献”但未验证 | Phase 7 前论文/文档只称为 planned/future 或 baseline |

### Phase 8：端到端集成与鲁棒性验证

目标：v3 跑通 Quest -> Python -> Unity anchor 全链路，并具备长时间运行诊断。

验收矩阵：

| 场景 | 目标 |
|---|---|
| 本机 fake stream + fake pipeline | 无硬件快速 CI/smoke |
| Unity Editor + Python smoke server | command/status/heartbeat 验证 |
| Quest 真机 + Python video probe | ZMQ data plane 稳定性 |
| Quest 真机 + Python real pipeline | pose result 输出 |
| Quest 真机 + Unity anchor | frame-aligned anchor 正确显示 |
| reset/reacquire 反复触发 | 无旧结果污染，无重复 reset |
| NATS server 断开/重连 | Unity/Python 清晰报错，恢复后继续 |
| camera_info 缺失/变化 | runtime 等待或刷新 K，状态正确 |
| frame history miss | 不应用错误 transform，状态可诊断 |

建议验证命令：

| 类型 | 命令 |
|---|---|
| Python v3 单测 | `pixi run python -m unittest discover -s src_v3 -p "test_*.py"` |
| Python 编译 | `pixi run python -m compileall src src_v2 src_v3` |
| Unity 编译 | `dotnet build "EgoAnchor_Unity\Assembly-CSharp.csproj" --no-restore` |
| 协议生成 | `.\EgoAnchor_Protocol\tools\generate_proto.ps1` |

### Phase 9：切换策略与旧代码收敛

目标：v3 稳定后再考虑成为默认链路，不提前删除 v1/v2。

切换条件：

| 条件 | 说明 |
|---|---|
| v3 Quest 真机端到端连续运行稳定 | 至少覆盖常用 demo 时长 |
| reset/reacquire/control 可用 | Unity 侧无需 Python 键盘 |
| v3 anchor 坐标正确 | frame-aligned world anchor 与 v1 行为一致或更稳定 |
| v3 diagnostics 足够定位问题 | 输入、标定、pose、status、heartbeat、anchor state 都可见 |
| v1 仍可作为 fallback | 切默认前保留旧入口 |

旧代码处理：

| 代码 | 处理 |
|---|---|
| v1 `src/object_tracking_server.py` | 保留稳定链路，直到明确退役 |
| v1 Unity `Assets/Scripts` | 保留旧主场景依赖 |
| v2 smoke/demo | v3 稳定后标记为 archived/reference，避免继续加功能 |
| `nats_example` | v3 吸收后可保留为历史实验或删除，不能作为正式入口 |

## 7. 保留、调整、废弃清单

### 7.1 建议保留

| 类别 | 项目 |
|---|---|
| 通信 | ZMQ PUB/SUB multipart `[topic_utf8, protobuf_bytes]` 视频数据面 |
| 通信 | NATS Core pub/sub + request/reply 消息/命令面 |
| 协议 | Protobuf generated Python/C# |
| 协议 | `subjects.v1.json` 唯一 channel registry |
| 运行时 | topic/latest-only drain |
| 运行时 | command ack 不代表执行完成 |
| 运行时 | request_id 幂等 |
| Python | single-owner `TrackingRuntime` |
| Python | 可切换 YOLOE-26/SAM3 mask backend + FFS + FoundationPose + optional Cutie 算法组合 |
| Python | K center-crop remapping 与 camera_info cache 思路 |
| Unity | `frame_id -> capture-time left camera world pose` |
| Unity | Python 输出 OpenCV camera pose，Unity 做 world anchor |
| Unity | processor chain 思路，Pose struct 必须返回处理结果 |
| 诊断 | stage/phase/depth/mask/timing/reliability HUD 和日志 |

### 7.2 建议调整

| 项目 | 调整 |
|---|---|
| v2 `AnchorCommandService` | 改为 registry/router/handler + command acceptor |
| v2 `NatsControlClient` | 拆为 connection、subscriber、request client、main-thread dispatcher |
| v2 `TrackingRuntime` | 拆出 command executor、status/heartbeat factory、runtime loop |
| mask backend | 保持默认 YOLOE-26，同时允许 `module.segmenter.type="sam3"` 显式切换到 SAM3；两个后端都输出统一 `SegmenterResult` |
| v2 reliability | Python 做 perception quality，Unity 做 anchor policy/controller |
| current proto | 非破坏性追加 reliability/diagnostic 字段，不重排字段号 |
| generated script | 支持 v3 输出目录 |
| Unity settings | endpoint/NATS URL 走配置或 PlayerPrefs，不长期写死 |

### 7.3 应当废弃或禁止恢复

| 项目 | 原因 |
|---|---|
| NATS 承载 stereo/camera_info | 已确定 ZMQ 视频专用 |
| MessagePack v3 正式协议 | v3 使用 Protobuf |
| Python server 主循环堆 `if topic/command` | 可维护性差 |
| NATS handler 直接操作 pipeline/GPU | 并发风险 |
| Unity receiver 直接修改 Transform | 线程和边界风险 |
| Python 输出 Unity world pose | 破坏 frame-aligned 责任边界 |
| 使用 pose 到达时 HMD pose | 会引入延迟错位，是历史核心坑 |
| WebRTC 图像流、NATS 图像流 smoke server | 不符合当前路线 |
| 自动替换旧主场景/旧入口 | 风险过高 |

## 8. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| NATS broker 部署依赖 | Unity command/pose/status 不通 | 提供清晰连接状态、heartbeat、启动脚本；ZMQ video probe 可独立运行 |
| Unity Quest IL2CPP 下 NATS.Net 兼容问题 | 真机无法用 command/pose message plane | Editor 先验，Quest 单独早测；transport 层隔离以便替换 NATS client 实现 |
| ZMQ PUB/SUB 首帧丢失 | camera_info 或 stereo 初期缺失 | runtime 状态 `WAITING_INPUT/CALIBRATION`，camera_info 低频重复发 |
| Pipeline 强依赖同步 GPU 操作 | NATS callback 被阻塞或并发风险 | 双执行域，handler 不碰 pipeline，runtime 单 owner |
| Reset/reacquire 旧结果污染 | Unity 应用 reset 前旧 pose | generation、frame_id、runtime reset 后发布 no-pose/status，Unity 可按 generation 忽略旧事件 |
| 坐标转换错误 | Anchor world pose 错 | 对照 v1，写单元测试和 fake pose 可视化 |
| Reliability controller 过度复杂 | 影响阶段交付 | Phase 6 先 raw + processor baseline，Phase 7 再引入 policy controller |
| v3 新目录导致重复代码 | 维护成本上升 | 只复制稳定算法 wrapper 思想；协议和生成脚本共享；v3 稳定后再归档 v2 |

## 9. 推荐首轮最小闭环

为了快速确认 v3 方向，首轮不要直接接 FoundationPose。建议先完成这个最小闭环：

```text
Unity v3 QuestStreamPublisher or fake sender
  -> ZMQ QuestStereoFrame + QuestCameraInfo
  -> Python v3 QuestStreamReceiver + LatestQuestInputStore
  -> Python fake TrackingRuntime emits fake PoseResult with same frame_id
  -> NATS PoseResult
  -> Unity v3 PoseResultReceiver
  -> FramePoseHistory lookup
  -> CameraPoseFrameAligner
  -> DynamicObjectAnchor raw/stable pose

Unity AnchorCommandClient.Reset
  -> NATS request
  -> Python command handler + dedup + queue
  -> TrackingRuntime applies fake reset
  -> AnchorStatusEvent RESET_APPLIED
  -> Unity clears local processors
```

这个闭环完成后，再接真实 perception pipeline。这样可以把通信、协议、frame alignment、NATS command、Unity 主线程、安全队列、状态事件全部提前验证，避免一开始被 GPU/模型问题掩盖架构问题。

## 10. v3 最终架构结论

v3 应采用“ZMQ data plane + NATS message/command plane + Protobuf contract + registry/router + single-owner runtime + Unity frame-aligned anchor runtime”的架构。

Python 侧重点是稳定地产生带诊断的 camera-space `PoseObservation`，并通过 NATS 发布 `PoseResult/Status/Heartbeat`。Python 不拥有 Unity world pose，不直接做 anchor transform。

Unity 侧重点是把低频、延迟、噪声、间歇失效的 camera-space pose 通过 `frame_id` 对齐到 capture-time camera world pose，再经过 gate/filter/predict/state machine 形成稳定 real-object anchor。

已有方案中最值得保留的是分层、注册表/装饰器、幂等命令、handler 轻量化、单一 runtime owner、frame_id 对齐。最需要修正的是任何把高频图像迁移到 NATS 的路线。最应废弃的是把网络、pipeline、状态、Transform 应用继续堆进单个入口或 MonoBehaviour 的做法。

后续实施应严格按阶段推进：先契约和 smoke，再数据面，再 runtime，再 anchor，再 reliability controller，最后才考虑替换默认链路。
